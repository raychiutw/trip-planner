---
name: tp-run
description: 建置並啟動本機開發伺服器，在瀏覽器預覽行程網站。適用於需要在發布前在本機確認變更效果時。
---

# tp-run

建置並啟動本機開發伺服器。

## 步驟
1. **執行建置**：執行 `npm run build`。
2. **啟動伺服器**：使用 `npx serve -l 3000` 背景啟動本機伺服器。
3. **預覽網站**：提醒使用者前往 http://localhost:3000 預覽行程網站。
