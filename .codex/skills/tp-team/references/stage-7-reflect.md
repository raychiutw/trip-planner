# Stage 7: Reflect

⚠️ 只有走到這一步，這個 change 才算真正完成。

## Step 1 — `/retro`（選用）

```
/retro
```

Retro Analyst 角色。
- 分析 commit history + 工作模式
- 做得好的 / 做得不好的 / 改善行動

## Step 2 — Archive

```
/opsx:archive
```

歸檔 OpenSpec change（等同 `npx openspec archive "change-name" -y`）。

─── ✅ 完成 ───
流程完成。這個 change 已歸檔，可以開始下一個。
──────────────────
