## 1. Issue 標題字級

- [x] 1.1 `.issue-item-title` font-size 改為 `var(--fs-callout)`
- [x] 1.2 `@media (min-width: 768px) .issue-item-title` font-size 改為 `var(--fs-title3)`

## 2. Body / Meta 字級

- [x] 2.1 `.issue-item-body` font-size 改為 `var(--fs-callout)`
- [x] 2.2 `.issue-item-meta` font-size 改為 `var(--fs-footnote)`

## 3. Reply 字級

- [x] 3.1 `.issue-reply` font-size 改為 `var(--fs-body)`
- [x] 3.2 `.issue-reply code` font-size 改為 `var(--fs-callout)`

## 4. Mode Pill 字級

- [x] 4.1 `.edit-mode-pill` font-size 改為 `var(--fs-callout)`

## 5. 驗證

- [x] 5.1 `npm test` 確認 css-hig.test.js 全過
- [x] 5.2 視覺檢查 light/dark 模式
