## 1. rules-json-schema.md 更新

- [x] 1.1 HotelSub 移除「格式一：簡單標籤」（label/text），只保留新格式（type/title/price/note/location）
- [x] 1.2 Meta 移除 `name` 和 `themeColor` 欄位定義

## 2. template.json 更新

- [x] 2.1 移除 `meta.name` 和 `meta.themeColor`
- [x] 2.2 加入 `meta.tripType: "self-drive"`
- [x] 2.3 hotel.subs 改為新格式範例（含 type/title/price/note/location）

## 3. render-trip.md 更新

- [x] 3.1 R7 加入 shopping category 7 類標準分類定義（超市、超商、唐吉軻德、藥妝、伴手禮、購物中心、Outlet），標明 timeline 景點購物與飯店購物共用
