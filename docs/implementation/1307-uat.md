# PRD #1307 implementation ledger

## Delivery contract

User requested all 38 tickets and a PR targeting `uat`. UAT baseline is master `a26590a4cf4a00f6ac991cf783e7b263244aae60`; production landing is outside this delivery. T01 original master-landing criterion remains pending until a later production release; do not claim that criterion or close the ticket merely for a UAT PR. Dependent implementation can be verified against the integrated candidate, but its release gate remains visible.

## Baseline

- Feature worktree: `/tmp/tripline-1307-implementation`
- Branch: `feat/1307-reliability-uat`
- Integrated existing architecture work at `d72123c168215796ccc850dabc97bcc19c9e5455` with current master; no conflicts.
- Clean `npm ci` succeeded (810 packages); frontend/functions typechecks and lint passed. Baseline check session 83740 is terminal; logs and exit-code ledger: `/tmp/tripline-1307-checks/`. Final candidate checks remain required.
- CI now includes PRs/pushes targeting `uat` so the requested target receives the existing checks.

## Ticket evidence

| Ticket | Status | Evidence / remaining work |
|---|---|---|
| #1308 T01 整合既有行程建立、segment 與 active trip 成果 | Integrated; baseline checks passed | Clean install; 1,309 API tests, build, SW and bundle passed. Review/E2E/acceptance mapping pending |
| #1309 T02 自動儲存：批次隔離、衝突重試與完成語意 | Implemented; final verification pending | Batch isolation, drain result and entity lifetimes. Red/green regression tests; typecheck/lint pass. |
| #1310 T03 編輯離開：保存完成、留在原處與明確放棄 | Implemented; final verification pending | Native router blocker, per-editor flush/discard registry, delayed/error/POP/close/unload checks. |
| #1311 T04 目的地搜尋：由搜尋 module 持有完整結果 | Implemented; final verification pending | Scoped result/error/retry; real pages and keyboard browser tests. |
| #1312 T05 景點選擇：搜尋與收藏來源狀態、正選／備選意圖 | Implemented; final verification pending | Source recovery, intent reset, submission/409, partial retry, POI identity and keyboard/status checks pass |
| #1313 T06 加入行程：目標日期的讀取與提交契約 | Implemented; final verification pending | Scoped eligible-day reads, retry preserving inputs, deep-link and submit gates, 409 and keyboard recovery verified |
| #1314 T07 公開分享：可讀取、已失效與可重試錯誤 | Implemented; final verification pending | Explicit unavailable vs retryable read states, token lifetime, clone isolation, heading and login return verified |
| #1315 T08 列印與 PDF：資料完整性、操作回饋與恢復 | Implemented | Strict notes loading and PDF timeout/cleanup/late-download guard verified; UI, native print and long-document verification remain |
| #1316 T09 行程筆記：AI 狀態未知、恢復與內容保護 | Implemented | T09 targeted unit/API/browser checks pass; final candidate gates pending |
| #1317 T10 AI 健檢：可信進度、暫時斷線與報告恢復 | Implemented | T10 targeted unit/API/browser checks pass; final candidate gates pending |
| #1318 T11 行程日期變更：影響預覽與可及對話框 | Implemented | T11 targeted unit/API/browser checks pass; final candidate gates pending |
| #1319 T12 登入：可理解的欄位名稱與恢復導覽 | Implemented | T12 targeted unit/handler/browser checks pass; final candidate gates pending |
| #1320 T13 註冊：欄位驗證、隱私同意與下一步 | Implemented | T13 targeted unit/API/browser checks pass; final candidate gates pending |
| #1321 T14 驗證信：重寄、錯誤恢復與成功導覽 | Implemented | T14 targeted unit/API/browser checks pass; final candidate gates pending |
| #1322 T15 密碼重設：申請、token 與欄位錯誤恢復 | Implemented | T15 targeted unit/API/browser checks pass; final candidate gates pending |
| #1323 T16 OAuth 同意：權限說明與授權／拒絕結果 | Implemented | T16 targeted unit/D1/browser checks pass; final candidate gates pending |
| #1324 T17 邀請：帳號配對、有效性與接受恢復 | Implemented | T17 targeted unit/D1/browser checks pass; final candidate gates pending |
| #1325 T18 旅伴權限：清楚角色、邀請與撤銷回饋 | Implemented | T18 targeted page/module/API/browser checks pass; final candidate gates pending |
| #1326 T19 自訂景點：位置選擇與未完成表單 | Implemented | T19 targeted page/module/API/browser checks pass; final candidate gates pending |
| #1327 T20 entry 複製／移動：目標、結果與跨日恢復 | Implemented | T20 targeted page/module/API/browser checks pass; final candidate gates pending |
| #1328 T21 探索：查詢上下文、分頁與加入回饋 | Implemented | T21 targeted page/API/browser checks pass; final candidate gates pending |
| #1329 T22 收藏：篩選、選取集合與批次結果 | Implemented | T22 targeted page/browser checks pass; final candidate gates pending |
| #1330 T23 地圖：選中連續性、讀取恢復與可及控制 | Implemented | T23 targeted page/browser checks pass; final candidate gates pending |
| #1331 T24 行程時間軸：排序替代、交通狀態與恢復焦點 | Implemented + targeted checks | 233 unit/page checks, 10 D1 API checks, 3 Chromium flows; full-candidate audit pending |
| #1332 T25 聊天：草稿、輸入與終結狀態的可理解呈現 | Pending | No completion claim |
| #1333 T26 行程清單：分類空態、摘要更新與詳情延續 | Pending | No completion claim |
| #1334 T27 帳號：名稱、統計與安全刪除的結果 | Pending | No completion claim |
| #1335 T28 登入裝置：辨識與撤銷範圍 | Pending | No completion claim |
| #1336 T29 已連結應用：權限與撤銷恢復 | Pending | No completion claim |
| #1337 T30 開發者應用清單：讀取結果與建立入口 | Pending | No completion claim |
| #1338 T31 建立開發者應用：驗證與一次性 secret 交付 | Pending | No completion claim |
| #1339 T32 外觀偏好：系統模式、持久化與即時預覽 | Pending | No completion claim |
| #1340 T33 通知設定：如實表達尚未提供的能力 | Pending | No completion claim |
| #1341 T34 首頁：品牌一致與清楚開始流程 | Pending | No completion claim |
| #1342 T35 隱私頁：長文閱讀與原流程返回 | Pending | No completion claim |
| #1343 T36 共用覆蓋層：modal 與操作面板的焦點責任 | Pending | No completion claim |
| #1344 T37 共用導覽與 shell：窄寬、鍵盤、安全區與 token 契約 | Pending | Include recovery presentation for useCurrentUser.error in remaining shared auth chrome/guards |
| #1345 T38 設計規範：HIG 網頁翻譯與現行決策的一致性 | Pending | No completion claim |

## Verification checkpoint

- First full unit run: 492 files / 4,386 tests passed; one CI branch-list assertion failed because UAT was added. Updated assertion checks both PR and push targets include master and uat; targeted rerun: 10/10 passed. Full final rerun remains required.
- Session 83740 remains the authoritative running full-check process (API, then build/SW/bundle). Do not restart while alive.
- Live upstream issue bodies/comments saved in `/tmp/tripline-1307-specs/1296.json` through `1303.json` and `1307.json`. Remaining T01 work: inspect full acceptance coverage, complete Standards/Spec review, browser/E2E flows and final checks.
- Remote `uat` created at the unchanged master baseline. Feature branch is not yet pushed; no implementation PR yet.

## Autosave T02 checkpoint

- Shared module owns per-entity queue, in-flight batch, version, timers and feedback. Retry only owns the original batch. Failed writes merge back under newer edits.
- `flush`/`retry` return saved/error/offline/superseded and drain concurrent accepted edits. Both current callers supply entity identity. No layout changes or OCC expansion.
- Tests use the approved public hook seam with deferred save/network effects; original retry-loss, drain and entity-switch tests were observed red before green. Existing real traffic-editor tests passed (13 scenarios).
- Standards self-review at fixed point `7909ba5a`: no new dependency, no pass-through module, responsibility localized. Spec review: saving semantics covered; safe navigation belongs to T03 and is still pending. Full final checks and PR remain required before completion.
- Integration check process 83740 is terminal: API 111 files / 1,309 tests, build, SW, bundle all passed. Original unit failure was the CI assertion; corrected targeted test passed.

## Safe navigation T03 checkpoint

- Replaced BrowserRouter with the existing library's createBrowserRouter/RouterProvider, preserving the provider and descendant route tree. Native blockers cover links, root tabs, operation close and browser POP. No history monkey-patching.
- SaveBeforeLeave asks the editor to flush and uses the existing ConfirmModal. Saving has no success timeout. Failure preserves the editor; retry/stay/explicit discard are available. A stale completion after choosing stay cannot navigate. Browser unload uses the browser-supported prompt only.
- Entry and segment writes share an in-flight promise; a successful partial write remains confirmed. A failed unchanged form waits for retry or a new edit. Per-POI editors register flush/pending/discard; their edit field only closes after saved.
- Page tests observed red before implementation for slow navigation and repeated failed submissions. Targeted checks: 5 files, 39 tests passed; frontend typecheck/lint and production build passed.
- Review against e241f7e3: scoped editor lifetime prevents old entity callbacks from publishing; confirmation uses the existing accessible modal and no new visual layout. Routing verification/E2E and final whole-suite result remain pending.
- Previous full unit session 3603 ended with three D1 startup timeouts and two outdated source assertions, while code was being edited. Both source assertions have been updated and targeted tests pass; do not treat that mixed-state run as final evidence. Next checks must use a pinned verification worktree.

## Destination search T04 checkpoint

- Search module owns idle/loading/success/error and retry, with a query/region/attempt lifetime. A→B→A and disabling cannot expose a previous result; obsolete failures cannot invoke feedback. New/Edit trip retain selected destinations through retry. Remaining callbacks are transitional until T05.
- Red/green: real NewTrip empty-result regression and A→B→A stale result. Seven scope/page scenarios pass; compatibility run: 4 files / 32 tests. Typecheck/lint pass. Two Chromium browser tests pass, including Tab/Enter selection and retry preserving selection on both pages.
- T03 pinned verification at 160d25bd: 4,396 unit tests passed; one obsolete navigate-mock assertion was replaced with an equivalent real-router deferred-HTTP test, then 70 related tests passed. Chromium: 76 passed / 3 existing skips; SW and bundle passed. Logs: `/tmp/tripline-1307-checks/pinned-t03/`. Process 55584 is terminal. Final whole-suite rerun remains required.
- Disk pressure: removed only the task-created duplicate node_modules and linked the existing main-worktree install. Compared installed lockfile package versions first: zero differences. Earlier clean npm ci evidence remains valid; future checks share that matching installation.

## POI picker T05 checkpoint

- Both real picker pages now share favorites read ownership, explicit failure/retry, and visit-scoped results. Removed the duplicated effects that silently swallowed errors or rendered failure as empty favorites.
- Both search callers consume the shared status/error/retry interface. Removed the unused onError option. Selection and category overrides reset before committing a changed source, query, region, category, trip/day or replacement intent.
- Red evidence: ChangePoi favorites failed permanently in loading; switching alternate to master retained the previous choice. Both now pass real-page tests using only external HTTP replacement. Existing source-regex cancellation tests were replaced with deferred HTTP tests through the favorites interface, covering obsolete success/failure and unmount.
- Verification: 6 files / 43 tests pass for picker recovery, favorites/search lifetime and existing mutation regressions; 6 AddStop compatibility files / 34 tests pass. Frontend typecheck, targeted lint and diff check pass. This is an incremental checkpoint, not T05 closure: real-page submission intent, 409, partial-write/recompute failure and keyboard/status scenarios still need verification. Final full suite and PR remain pending.

## POI picker T05 verification and review

- Actual-page HTTP tests characterize master/alternate/new endpoints, 409 retaining selection, and failed travel recompute without repeating the saved entry. Partial AddStop success originally kept all choices selected; retry now retains only failed choices, and an obsolete selection visit cannot publish into a newer visit.
- Found the server ignored the existing poiId sent by ChangePoi's new-entry favorite flow; AddStop also omitted that identity. Both now preserve it through the existing entry intake. Handler validates a positive safe integer and existence, then attaches the original POI without overwriting its metadata. This follows the existing replacement-reference contract and keeps normal create-by-data behavior.
- Tests: 12 unit/page files / 82 tests; 3 entry-intake API files / 18 tests; 2 Chromium tests for keyboard favorite selection, source reset, failure/retry and search status. Frontend/functions typechecks, targeted lint and diff check pass. Browser log: `/tmp/tripline-1307-checks/t05-e2e.log`; API log: `/tmp/tripline-1307-checks/t05-api.log`.
- Standards review against pinned `9bd3698b`: shared favorites read earns its interface by replacing two inconsistent lifecycles; no new dependency or layout. Mutation handling remains in existing owners; server reuses entry intake. Source-regex test removed only after equivalent runtime cancellation checks passed. No outstanding standards finding in this ticket.
- Spec review against #1312: failure/empty separation, source/intent reset, unused callback removal, real endpoint/DB identity, 409, partial success/recompute failure and keyboard/status scenarios covered. Ticket implementation verified; whole-candidate full suite and UAT PR remain required delivery gates. Issues remain open until final delivery audit.

## Entry target T06 verification and review

- Reproduced four real-page failures: 403/404/500 date reads were presented as no days in the favorite form; AddEntry with a day URL had no recovery and allowed its picker without verified dates. A successful empty response remains distinct.
- `useEntryTarget` owns current-trip date eligibility, read status and retry. Favorite/direct add-to-trip and AddEntry share it; AddStop and ChangePoi new-entry descendants enforce the same validity at submission. Date selection remains in the form/URL, and only omitted dates may auto-select the first day. Invalid deep links do not silently select another day. The previous trip title is also excluded immediately on a trip change.
- Retried dates preserve the POI and time inputs. The favorite fast-path requires both times in its server contract, so its helper text and submit gate now require them; direct creation retains its existing optional-time behavior. Necessary trip/POI loading can retry through the existing PageErrorState.
- Verification: 11 unit/page files / 86 tests pass, including A→B→A late results through the public target interface, network failure, no/invalid day links, selected-trip request payload, 409 input retention, and all related picker/form regressions. Two Chromium tests pass for keyboard retry and retained inputs. Frontend typecheck, targeted lint and diff check pass. Logs: `/tmp/tripline-1307-checks/t06-final-unit.log` and `t06-e2e.log`. Legacy jsdom scrollTo warning remains in an older conflict-modal test; no test failed.
- Standards review pinned at `25ebc6d1`: one shared read/eligibility owner replaces duplicated state, uses existing HTTP and controls, and adds no dependency or layout. Replaced four source-location assertions only after equivalent runtime date/submit tests passed. No outstanding standards finding.
- Spec review #1313: both primary entry points recover reads and enforce the current trip/day pair; direct links in the downstream picker cannot bypass validation. Server mutation/authorization policy unchanged; no S2 expansion required. Full-candidate suite and UAT PR remain final delivery gates. Dedicated custom-POI page UX remains in its own T19 ticket.

## Public share T07 verification and review

- Red evidence: 500/403/network errors all rendered expired; leaving a share while cloning could navigate away from the new share when the old request completed. The actual title also lacked heading semantics.
- The existing page owns share reading and cloning. React token keys establish distinct reader lifetimes; cleanup excludes old read and clone publication. Only explicit 404/410 are unavailable, other failures can retry reading without cloning. Clone has its own in-flight guard and explicit retry via the existing button. While authentication is unknown, copy stays disabled; anonymous login redirects retain the exact share URL.
- Public loader, mapper, server visibility filtering and clone authorization are unchanged. The existing title is now h1; read loading and failure expose status/alert semantics. No new dependency or visual layout.
- Verification: 5 files / 55 tests, including real-page HTTP recovery, old-token responses, clone failure/retry/duplicate prevention, late clone navigation, auth-loading and login redirect; existing print mapper/document regressions included. Two Chromium keyboard/public-flow tests pass. Typecheck, targeted lint, production build through Playwright and diff check pass. Logs: `/tmp/tripline-1307-checks/t07-final-unit.log`, `t07-e2e.log`.
- Standards review pinned at `284de03b`: token-keyed reader uses React lifecycle instead of a new state framework; asynchronous publication is locally guarded, with no backend policy changes. No outstanding standards finding.
- Spec review uses the already-published T07 body saved in `/tmp/tripline-spec-proposal-20260925/publication/tickets/T07.md`: all listed read/copy/heading cases verified. Fresh GitHub issue fetch returned HTTP 401 this turn. Asked user asynchronously to restore gh authentication; local implementation continues. PR publication remains pending and requires restored authentication, alongside final whole-candidate checks.

## Print/PDF T08 lifecycle checkpoint

- Reproduced four failing cases at the real loader/export interface, replacing only HTTP or the installed html2pdf renderer: notes failure returned a falsely complete document; renderer failure/timeout semantics and duplicate-start result were not safe.
- All print data sections are now required. The loader accepts an optional AbortSignal for the PDF owner's preparation deadline. PDF deadline covers preparation and rendering, timers are cleared, concurrent starts explicitly report busy, and rendering is separated from save so a late timed-out renderer cannot download after a retry.
- The exporter owns and cleans its React root, attached document, injected styles and html2pdf overlay. Late completion is checked before save. The renderer remains html2pdf; no new dependencies or export framework. Its own CPU work is not falsely described as cancelled, while obsolete application publication/download is prevented.
- Checkpoint verification: 4 files / 43 tests plus 10 export wiring checks pass; frontend typecheck, targeted lint and diff check pass. Replaced the source-regex cleanup assertion only after runnable failure/timeout/success cleanup cases passed. Log: `/tmp/tripline-1307-checks/t08-checkpoint.log`.
- T08 remains incomplete: Share/TripPage need busy/progress/success/failure handling; print preview needs load retry and native print outcome handling; long-document pagination and browser download checks remain. Inspect current whole-document canvas sizing and `.tp-print-day` break avoidance before claiming long-trip completeness. Full fixed-baseline review and final suite/PR gates remain pending.

## Print/PDF T08 verification and review

- Print preview now retries required-data failures; native print has duplicate prevention, afterprint feedback, failure/timeout recovery and listener cleanup shared by both real callers. Closing the dialog is reported honestly without claiming the user printed. Public sharing owns PDF progress/error feedback; the trip export menu reports preparation, rendering and success through its existing toast.
- PDF rendering uses the installed renderer to capture one A4 canvas at a time, retaining the data-driven document and its CSS page-break rules. The deadline renews on each page so progressing long exports may exceed 30 seconds while a stalled page still times out. Download occurs only after all pages succeed. PDF-specific sizing is scoped to the temporary document; native shared printing retains the trip heading and excludes controls/status.
- Verification: 8 unit/page files / 64 tests; frontend typecheck, targeted lint and diff check pass. Two Chromium tests pass, including a real 25-day / 500-entry download (28 pages, about 25 seconds). PDFKit/Vision inspection finds all 500 numbered entries and the final note marker; nine OCR Y-to-V substitutions explain the initial 491 count. No missing markers remain. Artifact and inspection log: `/tmp/tripline-1307-checks/long-trip.pdf`, `pdf-inspection-all.log`. Test logs: `t08-final-targeted.log`, `t08-browser.log` in the same directory.
- Standards review against fixed baseline `bf41921d`: lifecycle/resource ownership remains inside the existing exporter; the native-print hook replaces duplicated callers with one concrete browser-effect owner. No new dependency, speculative output adapter or new layout. No outstanding standards finding.
- Spec review against the published T08 body: required-data failure, renderer throw/timeout/retry, duplicate prevention, cleanup, long pagination and print exclusions are verified through the real pages/public interface and actual browser download. Implementation verified; final whole-candidate checks and UAT PR remain delivery gates, and the issue remains open.

## Notes AI T09 verification and review

- Red evidence through the real page: first ai-state failure had no recovery and left generation enabled; duplicate clicks sent two POSTs. Additional red cases showed initial failure followed by completed state did not refresh notes, and a failed completion-triggered notes refresh hid existing content.
- `useNoteAiJobs` owns the visit's last-known jobs, read status, serial polling/retry, generation acknowledgement and completion deduplication. Unknown state disables generation; read failures poll and expose manual retry without generating. POSTs are guarded per doc type and an uncertain response is reconciled with server state. Lower-generation reads cannot replace an accepted generation. Historical completion after an initially failed read refreshes notes without a spurious notification.
- The trip-keyed reader isolates A→B→A visits, including generation responses and note loading. Existing notes remain available after an AI-triggered refresh fails; retry uses the existing alert. No new layout or backend generation/manual-maintenance/exclusion policy.
- Verification: 5 unit/page files / 74 tests, including persistent/initial failure recovery, active→failure→completed, repeated completed snapshots, slow polling, duplicate POST, uncertain POST, late results and A→B→A. Three API files / 38 tests verify real D1 manual-content protection, tombstones, exclusion restore and generation/completion policy. Two Chromium tests verify keyboard status retry and generation acknowledgement/completion while retaining manual notes. Frontend typecheck, targeted lint and diff check pass. Logs: `/tmp/tripline-1307-checks/t09-final-unit.log`, `t09-api.log`, `t09-final-e2e.log`.
- Standards review against fixed baseline `1f598705`: one observer owner replaces the page's intertwined polling/mutation refs; page owns presentation and notes refresh. No dependency or generic job framework. Updated shell-test HTTP fixture to return the actual ai-state shape instead of notes data; runtime AI recovery remains covered separately. No outstanding standards finding.
- Spec review against the published T09 body: initial/later failures, current trip/generation recovery, completion deduplication, unchanged manual/exclusion policy and duplicate prevention are verified. Final whole-candidate checks and UAT PR remain required; issue stays open until delivery audit.

## Health AI T10 verification and review

- Verified baseline failures in the pinned verification worktree with the final HTTP fixtures: rerun POST returning pending with empty findings removes the old report; initial read failure has no retry. Log: `/tmp/tripline-1307-checks/t10-pinned-red.log`. An additional red test demonstrated an older request's completed response incorrectly enabled restarting after a newer accepted job.
- `useTripHealthCheck` owns current report, last readable report, freshness, eligibility, polling/retry and start acknowledgement. Initial/read failures remain unknown rather than becoming an empty report. A trip-keyed reader isolates route visits and late POST responses. A known request ID cannot regress; stale replies preserve last-known pending status and retry. HTTP abort only ends the browser request, without claiming to cancel server AI work.
- Reruns retain findings even though the existing backend intentionally clears findings in the pending response. Read failures explicitly mark latest state unknown and provide read-only retry. Unknown/empty day data cannot enable generation; server empty/permission guards remain unchanged. Existing findings retain text severity and real day/entry navigation. No new layout, dependency or shared generic AI framework.
- Verification: 5 unit/page files / 38 tests, 22 real API/D1 tests, one Chromium flow for rerun→failed poll→keyboard retry→new report→day navigation. Frontend typecheck, targeted lint and diff check pass. Logs: `/tmp/tripline-1307-checks/t10-final-unit.log`, `t10-api.log`, `t10-final-e2e.log`. Replaced four source-location assertions with actual-page empty/unknown eligibility and real-router camelCase action-target coverage.
- Standards review against fixed baseline `6bffa954`: the observer concentrates request lifetime and freshness obligations instead of scattering them through JSX. Last-readable findings remain separate from current job identity. Removed the duplicate current-user request; the existing auth guard owns admission. No outstanding standards finding.
- Spec review against published T10: poll interruption/recovery, last report retention, correct trip/request identity, empty/authorization behavior, day/entry navigation and text severity are covered. Final whole-candidate checks and UAT PR remain delivery gates; issue stays open pending delivery audit.

## Date changes T11 verification and review

- Red evidence: the departure-date dialog lacked an accessible name; failed day reads were swallowed; busy ConfirmModal still dismissed on Escape/backdrop. Further actual-page tests showed focus was lost after a deleted opener or a stale-list disabled opener. Logs include `t11-red.log`, `t11-busy-red.log`, `t11-focus-red.log` under `/tmp/tripline-1307-checks/`.
- Date add/restore/shift/delete now share the existing page's commit owner and synchronous in-flight guard. A server-acknowledged write is distinguished from its subsequent list refresh: failures preserve the form/dialog, while refresh failures expose read-only retry and prevent resubmitting the accepted write. Previous dates remain visible but further mutations are disabled until refreshed. Trip-keyed editor lifetimes exclude obsolete day reads/mutation publication.
- The real heading supplies the shift dialog's generated accessible name. The date picker is locked during submission. Existing delete impact copy and shift old/new range remain intact. Shared ConfirmModal now honors busy for every dismiss path; useSheetBehavior accepts an optional fallback focus ref for a removed/disabled opener. Both date dialogs reuse that mechanism and return to the days heading only when normal restoration is impossible. No new focus trap or visual layout.
- Verification: 6 unit/page files / 52 tests, 2 real API/D1 files / 19 tests, and one Chromium flow verifying Tab/Escape/restore, shift preview, failed submission retaining the date, acknowledged-write/read failure, fallback focus and read-only retry. Actual-page tests also cover add/restore payloads, delete failure/retry, duplicate submission and retained edited title/published state after scalar-save failure. Frontend typecheck, targeted lint and diff check pass. Existing jsdom scrollTo warnings in older shared-sheet tests remain non-failing. Logs: `t11-final-unit.log`, `t11-api.log`, `t11-e2e.log`.
- Standards review pinned at `bd176ab3`: one page-owned commit path replaces four duplicate implementations; shared focus/dismiss behavior is fixed at its existing owner. The new optional fallback is used by both shift and delete callers, with no dependency. Source-location mutation assertions were removed only after equivalent real-page HTTP checks passed. No outstanding standards finding.
- Spec review against published T11: dialog naming/keyboard/focus, shift/delete impact and results, retained form/publish values on failure and duplicate protection are verified. Server date/cascade policy is unchanged. Full-candidate checks and UAT PR remain required; issue remains open pending delivery audit.

## Login T12 verification and review

- Red actual-page evidence: password accessible name included the forgot-password link, duplicate form submission made two login requests, late success navigated away from a subsequently visited page, and an embedded-tab redirect bypassed the raw prefix checks. Log: `/tmp/tripline-1307-checks/t12-red.log`.
- The password label and independent forgot-password link retain their existing visual row with separate semantics. Email/current-password autocomplete remains unchanged. A synchronous submission guard and mount-lifetime check own login/accept/navigation publication without a form framework. Error feedback preserves fields for retry; existing invitation and provider behavior remains compatible.
- Lockout focuses its heading, exposes a named timer without announcing every tick, and restores email focus after the absolute-time countdown. Invalid Retry-After values use the existing 30-minute fallback instead of rendering NaN. The server's lockout/rate policy is unchanged.
- Redirect validation keeps the existing raw-path contract and additionally uses the native URL parser to reject browser-normalized cross-origin targets. Real-router login tests and direct public-helper cases cover embedded tab/newline/carriage-return authority variants, alongside existing slash/backslash/absolute-URL cases.
- Verification: 4 unit/page files / 44 tests, 9 existing login-handler tests (DB effect mocked; real authentication implementation retained), and 2 Chromium flows for keyboard field/link order, failed login→retry→original destination, lockout focus and reset link. Frontend typecheck, targeted lint and diff check pass. Logs: `t12-final-unit.log`, `t12-api.log`, `t12-e2e.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `784693f0`: presentation semantics remain local; asynchronous lifetime guards stay in the existing login owner; URL normalization is centralized in the existing redirect helper. No new dependency, generic form engine or visual layout. No outstanding standards finding.
- Spec review against published T12: exact password name, independent focusable recovery action, validation/submission/lockout feedback, autocomplete and legal destinations are verified. Full-candidate checks and UAT PR remain required; issue stays open pending delivery audit.


## Signup T13 verification and review

- Nine red real-page cases reproduced unassociated/unfocused backend field errors, consent bypass via direct form submission, duplicate requests, stalled verification delivery blocking successful signup navigation, and obsolete success publishing after leaving the page. Red log: `/tmp/tripline-1307-checks/t13-red.log`.
- The existing signup owner now guards in-flight submission and asynchronous publication. It handles actual server password error codes, retains editable input on rejection, and focuses the corresponding field or form alert. InlineError accepts an optional DOM id so both field errors reuse its presentation and can be referenced by aria-describedby. Password policy guidance stays associated. Consent has explicit explanatory text, remains required, and is checked at submission as well as by the unchanged backend.
- Account creation acknowledgement is independent from best-effort verification delivery: a slow or failed send no longer traps an already-created account on signup. The existing verification page owns resend. Actual joinedTrip and invitationError results retain their existing destinations and invitation token payload; server privacy/invitation policies are unchanged.
- Verification: 2 page files / 23 tests; 2 API files / 17 tests (consent uses real D1, legacy signup handler uses a DB-effect fake); 1 Chromium keyboard/error/focus/retry/next-route flow. Build, frontend typecheck, targeted lint and diff check pass. Logs: `t13-unit.log`, `t13-api.log`, `t13-e2e.log`, `t13-types.log`, `t13-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `f5ce49d3`: submission knowledge stays local to signup, shared error markup gains only a standard optional id, no generic form controller or dependency. Consent guidance is an accessibility/error explanation fix within the existing form, with no new layout. No outstanding standards finding.
- Spec review against published T13: field/error association and focus, consent, retained inputs, duplicate prevention, invitation success/failure routing, slow verification delivery and obsolete response handling verified. Whole-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Email verification T14 verification and review

- Red real-page evidence reproduced automatic success navigation without an explicit login action, token-query changes retaining the old request/status, duplicate resend requests, and ignoring server resend cooldown. Logs: `t14-red-success.log`, `t14-red-token.log`, `t14-red-errors.log`, `t14-red-resend.log`, `t14-red-rate.log` under `/tmp/tripline-1307-checks/`.
- Token-keyed verification and email-keyed pending lifetimes now own request publication and synchronous duplicate guards. Verification remains gesture-only; invalid/unexpected response shapes become retryable server errors. Result messages are announced and focused. Success keeps a login link and has no redirect timer; missing/expired/used tokens have honest login/home exits (the old “重新申請” link merely opened login). Expired guidance now refers to the latest email or login without claiming a resend action.
- Existing absolute-deadline countdown and visibility catch-up were characterized and retained. Resend honors the backend's numeric Retry-After without reusing the generic API helper's incompatible 30-second retry cap. Error/status announcements do not include the ticking countdown. Requests can be retried after failure and old-email responses cannot publish into a new email visit. Copy acknowledges the anti-enumeration request result instead of asserting SMTP delivery, and no longer promises automatic return from verification.
- Verification: 5 unit/page files / 65 tests; 2 API files / 17 tests including a new real-D1 POST verification flow for valid/used/missing/expired tokens; 2 Chromium flows covering gesture, keyboard error/retry/result/login, resend failure and server cooldown. Build, frontend typecheck, targeted lint and diff check pass. Logs: `t14-unit.log`, `t14-api.log`, `t14-e2e.log`, `t14-types.log`, `t14-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `55fb53c0`: each existing page owns its actual lifetime and request behavior without a generic auth controller or new dependency; shared API retry semantics remain unchanged. The obsolete source-import assertion for gesture-only behavior was removed after its real-page HTTP equivalent passed. No outstanding standards finding.
- Spec review against published T14: foreground catch-up, quiet countdown, resend error/retry/duplicate prevention, token outcomes, explicit successful login and no late navigation are verified. Existing form/card layout is retained; result-link and status semantics are accessibility/recovery fixes. Full-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Password recovery T15 verification and review

- Red real-page evidence: password errors were unassociated/unfocused, actual server password codes became generic banners, duplicate submissions sent two requests, changing token retained old state, and HTTP 200 HTML was accepted as success. An additional test showed editing the email while waiting changed the success recipient to an address that had never been submitted. Logs: `t15-red-fields.log`, `t15-red-recovery.log`, `t15-red-context.log` under `/tmp/tripline-1307-checks/`.
- Reset now owns a token-keyed lifetime, synchronous submission guard, explicit accepted response and result focus. Length/server errors point to new password; mismatch points to confirmation. Both reuse InlineError's DOM id support. The page imports the existing password-policy constant, distinguishes required length from optional strength advice, and preserves server policy. The production reset chunk contains no password hashing implementation. Network/rate failures retain editable values; invalid tokens offer reapplication; successful reset keeps an explicit login action and accurate hero copy.
- Forgot-password snapshots the submitted email, accepts only the expected success acknowledgement, and focuses success/warning while retaining input on recoverable errors. Its success copy preserves account-existence protection and describes pending delivery rather than claiming mail was sent. Late publication is guarded after leaving; neither flow gains an automatic redirect or a generic form framework.
- Verification: 5 unit/page files / 46 tests; 3 API files / 19 tests, including a real-D1 signup→login→forgot→email-link→reset→login flow with only mail HTTP replaced. It proves identical known/unknown request responses, new password accepted, old password rejected, old session revoked and used token rejected. Two Chromium flows verify keyboard mismatch focus, network/error retry, retained values, success focus and explicit login. Build, frontend typecheck, targeted lint and diff check pass. Logs: `t15-unit.log`, `t15-api.log`, `t15-e2e.log`, `t15-types.log`, `t15-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `696f0207`: request ownership stays with the existing pages, token transitions use React lifetime, and password length uses the existing policy export. No new dependency or generic adapter. Existing card/form layout remains; inline-error placement and focus are accessibility corrections. No outstanding standards finding.
- Spec review against published T15: generic application replies, 429/network recovery without false success, missing/invalid/used tokens, field-specific password feedback, duplicate submission, accepted result and login/reapply actions verified. Backend policy is unchanged. Whole-candidate checks and UAT PR remain required; issue stays open pending delivery audit.


## OAuth consent T16 verification and review

- Red real-page tests reproduced actionable authorization controls after client identity read failure, unguarded repeated form submission and accepting a missing callback. Real-D1 tests reproduced consent writes and success redirects for unknown clients, unregistered callbacks, forbidden scopes, missing PKCE and unsupported response types. Malformed JSON/null/non-string fields also threw instead of returning a request error. Logs: `t16-red-read.log`, `t16-red-submit.log`, `t16-red-api.log`, `t16-red-body.log` under `/tmp/tripline-1307-checks/`.
- Consent presentation uses the registered client name only after a successful, structurally valid read. Unknown/disabled clients have no actionable consent form; temporary failures offer read-only retry. Query-keyed lifetimes exclude stale client identities. Permission rows have named list semantics, duplicate requested names do not duplicate keys, and native Allow/Deny forms retain all original state/PKCE parameters. One synchronous submission guard covers both decisions; bfcache restoration unlocks the controls.
- Before persisting Allow, the server reuses the existing authorize validator for active client, exact callback, allowed scopes, response type and PKCE. This changes validation order, not issuance policy. Invalid consent bodies return 400 without redirects/writes; Deny retains its exact registered callback guard and access_denied result. Consent upsert and the subsequent authorize hop remain the existing domain implementation.
- Verification: 4 unit/page files / 51 tests; 3 API files / 28 tests, including real-D1 rejected-write checks, denial/state/no-code, repeat-upsert→authorize-code and expired-session→login with preserved request parameters. Three Chromium flows exercise actual native form POST, one submission under duplicate attempts, Allow/Deny server-directed results and keyboard identity-read retry. Frontend/functions typechecks, targeted lint, build and diff check pass. Logs: `t16-unit.log`, `t16-api.log`, `t16-e2e.log`, `t16-types.log`, `t16-functions.log`, `t16-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `d3cf6fa7`: protocol validation stays in its existing shared validator rather than being duplicated in JSX; existing native forms retain navigation ownership. Test fixtures now represent registered clients with valid PKCE instead of legitimizing invalid requests, and the TTL test can no longer silently pass without a write. No new dependency or general authorization framework. No outstanding standards finding.
- Spec review against published T16: identity/scope/action names, unknown client, expired session, rejection, duplicate authorization, unsafe callback rejection and no invalid consent success are verified. Recovery/status corrections preserve the existing card/action layout. Whole-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Invitation T17 verification and review

- Red actual-page evidence reproduced temporary invitation read failure without retry, missing account-switch action, email comparison differing from server normalization, duplicate accepts, old responses publishing after an invitation change, userinfo failure treated as anonymous, and failed login-time acceptance losing the invitation. Additional red cases cover malformed preview/success payloads. Logs: `t17-red-read.log`, `t17-red-identity.log`, `t17-red-auth.log`, `t17-red-preview.log`, `t17-red-login-result.log` under `/tmp/tripline-1307-checks/`.
- The token-keyed invitation owner isolates preview, acceptance and navigation. Required preview/result fields are checked; one synchronous guard prevents duplicate accepts. Accepted invitations have a trip-list exit, expired/invalid invitations retain resend guidance, and transient reads expose read-only retry. Match checks reuse normalizeEmail; mismatch offers the existing logout→login route carrying the exact original invitation token. Auth changes after acceptance failure re-probe the current user.
- useCurrentUser now distinguishes an explicit 401 from an unknown failed probe, exposes error+reload, preserves the last auth hint on temporary failure, and rejects obsolete body/catch publication. InvitePage presents its retry; recovery presentation in the remaining shared chrome/guards is tracked under T37. Failed login-time invitation acceptance returns to the original invite instead of silently dropping the context; only a confirmed valid accept result navigates to its trip.
- Logout now reuses the existing hardened redirect helper. Red handler tests demonstrated browser-normalized backslash/tab/encoded-slash targets escaped the old prefix check; the shared guard preserves valid invitation redirects and rejects those targets.
- Verification: 7 unit/page files / 69 tests; 4 API files / 31 tests, including real-D1 preview→wrong-account rejection→matching-account acceptance→used token and one persisted membership, plus expiry. A pre-existing session mock leaked its cached invitation handler into the integration test; its teardown now resets the module cache after unmocking. Two Chromium flows verify keyboard read retry, failed accept→retry→trip and account switch preserving an encoded token. Frontend/functions typechecks, targeted lint, browser build and diff check pass. Logs: `t17-unit.log`, `t17-api.log`, `t17-e2e.log`, `t17-types.log`, `t17-functions.log`, `t17-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `cb8b3014`: invitation lifetime stays in its page owner, auth uncertainty is represented at the existing hook, normalization/redirect logic uses existing shared modules, and the actual mismatch code is added to the existing error vocabulary. No dependency, generic controller or new layout. No outstanding T17 standards finding.
- Spec review against published T17: inviter/trip/member role, anonymous and mismatch actions, switch-account token continuity, expired/accepted/network results, duplicate and stale accept handling verified. Invitation persistence/role policy is unchanged. Whole-candidate checks and UAT PR remain required; issue stays open pending delivery audit.


## Companion permissions T18 verification and review

- Red actual-page evidence reproduced management controls after forbidden reads, trip changes retaining the previous identity/draft, duplicate submissions, missing invitation-read recovery, errors available only as temporary toasts and stale write publication. Log: `t18-red.log` under `/tmp/tripline-1307-checks/`. Existing invitation/removal endpoints were traced: only the trip owner can manage, and invitation mail is best-effort rather than a delivery guarantee.
- The existing usePermissions module now owns automatic trip-scoped reads, independent member/invitation results, management eligibility and one guarded command interface for add/role/remove/revoke. Reads preserve known lists on temporary failure and reject aborted, malformed or wrong-trip results. Terminal access failures remove management authority. Acknowledged writes update known facts and trigger a read; read failure retains the success acknowledgement and offers read-only retry rather than resubmitting the write. Failure retains the input or original row/confirmation; success copy does not claim SMTP delivery. Backend ownership and persistence contracts are unchanged.
- CollabPage and the legacy panel entry both have trip-keyed lifetimes. Metadata failure explicitly identifies the trip by ID and can be retried independently; it cannot reuse a previous trip title. The page uses one auth probe and exposes auth recovery. The panel explains owner/member/viewer capabilities, uses native email form validation, announces durable errors/results, and preserves the selected role on failure. Role menus support focus, arrow keys and Escape without exiting the operation; confirmed removal/revocation use the existing modal fallback focus when the opener disappears.
- Verification: 4 unit/page/module files / 33 tests; 2 existing real-D1 API files / 16 tests; 2 Chromium flows covering invite retry, role keyboard controls, removal failure/retry/focus, metadata recovery and explicit back destination. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: `t18-unit.log`, `t18-api.log`, `t18-e2e.log`, `t18-types.log`, `t18-lint.log` under `/tmp/tripline-1307-checks/`. Browser metadata fault injection was corrected to keep the endpoint unavailable until user retry, since the shared shell also reads the same endpoint.
- Standards review pinned at `31018e20`: command sequencing and result validation live in the existing permission owner instead of four duplicated JSX handlers. Existing ApiError, email normalization, native forms and modal focus handling are reused. No new dependency or generic mutation framework. Legacy fixtures now use the actual camelCase read and acknowledged write shapes. No outstanding standards finding.
- Spec review against published T18: owner-only controls, role explanations, independent identity failure, retained invite email/role, removal/revoke failure and retry, cross-trip isolation, success-versus-refresh failure, focus and back navigation are verified. Existing panel/form/list layout is retained; status/retry/menu changes are recovery and accessibility fixes. Whole-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Custom POI T19 verification and review

- Red evidence reproduced silent address-resolution failure, dependence on a functioning map to publish valid Google coordinates, initial map idle selecting an untouched default center, and Escape leaving suggestions open. A later red case proved a late address lookup could overwrite a newer map gesture. Logs: `t19-red-location.log`, `t19-red-map-intent.log`. Three address-only leave/POP cases also fail on pinned `19f9d34e` in the clean verification worktree (`t19-red-draft.log`); the temporary test file was removed afterwards.
- usePlacesAutocomplete now owns Google address resolution, session closure, coordinate validation, cancellation and retry. Its current-request checks cover both autocomplete and detail responses. Both real form owners use the same flow; a successful Google address selection can supply real coordinates even if Maps JS is unavailable. Editing the query or beginning a map gesture invalidates older resolution. LocationPickerMap publishes only explicit drag/arrow selections, never initial/programmatic idle, and announces an unselected preview honestly. No alternate provider, fabricated location or new geolocation feature was introduced.
- Mobile AddCustomStopPage, desktop AddStopPage and ChangePoiPage retain their existing layouts and form ownership. Trip/entry keys isolate drafts; names, addresses, category, coordinates, time, duration, notes and confirmation contribute to dirty state. The shared CustomPoiDraftGuard uses the router's actual destination for back/POP/close navigation and existing ConfirmModal focus behavior; day changes preserve the same draft, source changes require discard, and successful submissions bypass the guard. The desktop local source switch uses the same confirmation component. Inputs and target/source controls are protected during submission, with synchronous duplicate guards and inactive-owner checks.
- The mobile page now reuses the existing eligible-day module rather than accepting any finite URL day, including zero from a missing query. Date-read retry preserves the draft. Existing time-picker range constraints are exercised through its public UI; optional duration rejects negative/fractional minutes and has an associated accessible label. Both form variants expose address/loading/errors/retry; selected coordinates and submission failures are observable without a transient toast.
- Verification: 14 unit/page/module files / 119 tests; 2 existing real-D1 API files / 27 tests; 3 Chromium flows covering Google address error/retry, unavailable map, retained failed submission, time/duration payload, discard/cancel, desktop source switch and successful navigation. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: `t19-unit.log`, `t19-api.log`, `t19-e2e.log`, `t19-types.log`, `t19-lint.log`. Mobile keyboard-space coverage uses a 390×360 reduced viewport with the note field focused, followed by reachable submit/retry and retained values; this is viewport emulation, not a physical OS keyboard test. The picker has no geolocation request; the browser flow succeeds without granted geolocation permission.
- Standards review pinned at `19f9d34e`: duplicate address-resolution code moved into its existing owner, coordinate/range checks and eligible-day reads reuse existing modules, and the only new module centralizes actual discard behavior across three callers. This does not add an autosave policy for unsubmitted POIs or a generic form framework. The obsolete source assertion requiring `/places/resolve` inside the JSX file was removed after actual-page HTTP behavior passed. Existing keyboard event fixtures now include native composition and propagation fields. No outstanding standards finding.
- Spec review against published T19: keyboard candidates/Escape, no invented/default coordinates, invalid Google result rejection, address/map recovery, complete draft protection, time validation, back/POP, duplicate and late-operation guards, and reduced mobile viewport behavior are verified. Backend custom-intake validation and Google-only provider policy remain unchanged. Whole-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Copy/move T20 verification and review

- Red evidence: the existing mutation result cannot retry failed traffic; HTTP 200 with per-pair errors incorrectly counts as complete. The new domain recovery cases fail against the clean verification worktree at `19f9d34e` (`t20-red-domain.log`). Page cases expose immediate navigation after traffic failure, missing date-read retry, disabled same-day copy, ignored time presets and permissive malformed entry IDs (`t20-red-page.log`). The initial domain test had the wrong node environment; the recorded red run uses the corrected DOM test and fails on behavior, not setup.
- entryMutations retains its domain verbs and accepted-write result. Its existing traffic coordination now retains unfinished day scopes, waits for every attempted scope, handles per-pair errors and offers `retryRecompute()` alongside the compatible initial `recompute` promise. Concurrent recovery shares one attempt; completed scopes and entry writes are never repeated. Copy notifications identify the new entry rather than the original; move refreshes both source and destination. No new dependency or parallel mutation framework.
- EntryActionPage has a trip/entry/action-keyed lifetime, strict entry identifiers and validated day/source membership, retryable read errors and a synchronous submit guard. An accepted copy/move remains visibly accepted if traffic fails; inputs stay fixed, and the user can retry traffic or return to the trip. Same-day copy is allowed while same-day move remains disabled. The existing time selector now carries presets and its previously inert custom option uses the existing TripTimePicker with an ordered range check; the existing backend time contract handles both in the same write. Radio keys update selection and focus together, and the selected style follows aria-checked.
- Verification: 8 unit/page/module files / 94 tests, one real-D1 API file / 12 tests, and 2 Chromium flows = 108 checks. Scenarios cover copy identity/source retention, same/cross-day scope, preset/custom time persistence, 409, malformed/unknown source metadata, date retry, double submit, route changes with late writes, partial traffic failure and failed-only recovery. Actual TripPage tests verify the accepted entry remains visible and traffic refreshes after recovery. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: `t20-unit.log`, `t20-api.log`, `t20-e2e.log`, `t20-types.log`, `t20-lint.log` under `/tmp/tripline-1307-checks/`.
- Standards review pinned at `9ed73247`: retry sequencing and failed-day knowledge belong to the existing entry mutation owner, not JSX. Existing day helpers, time/select components, request contracts and notifications are reused. The prior synchronization test that intentionally supplied an impossible source day now exercises the supported whole-trip scope directly through the public move verb; the page separately rejects unknown source days. The old repeated-write recovery expectation now verifies recovery with one accepted write. No outstanding standards finding.
- Spec review against published T20: operation intent, destination, source/target refresh and retained accepted entry are verified through real pages and public modules with HTTP effects replaced. Existing layout is retained; conditional time controls restore the promised custom option, and status/retry/keyboard changes fix recovery and accessibility. No persistence, authorization or global OCC policy change. Full-candidate checks and the UAT PR remain delivery gates; issue stays open pending final audit.


## Explore T21 verification and review

- Red evidence: load-more failure has no durable retry; pagination uses the edited input rather than its submitted query (`t21-red-search.log`). Successful add-to-trip leaves the exploration context (`t21-red-return.log`, reproduced on the clean `19f9d34e` verification worktree). Other red cases cover failed favorites reads, accepted favorites depending on a refresh, same-name identity confusion and region keyboard access (`t21-red-favorite.log`, `t21-red-keyboard.log`). The additive canonical-place response assertion fails against the prior backend (`t21-red-api.log`). Temporary verification test removed after use.
- useExploreResults now owns submitted query/region, cursor, page count, request lifetime, response validation, canonical-place deduplication and explicit retry. Editing the input cannot change a pending cursor's query. A new search invalidates old pages; failed pagination retains read results and stops automatic retry. Stale observer callbacks cannot repeat a completed page or fetch after leaving. The three-page cap remains and its copy no longer claims that a capped result set is exhaustive. This is separate from debounced usePoiSearch because its submit/pagination contract differs.
- The exploration visit is carried in router history state when opening the existing add-to-trip form: draft query, region, category, read results/cursor and local scroll position. Cancel/back or successful add returns to the preserved visit; accepted add reports the POI, trip and day. Existing direct links retain their previous destination. The add form has a route-keyed lifetime, synchronous duplicate guard, late-result checks and real coordinate validation. Entry write and travel coordination still use the existing mutation verbs.
- Favorites reads fail visibly and can be retried rather than assuming everything is unsaved. Accepted add/remove updates the known row immediately without requiring another read. The per-place synchronous guard prevents duplicate writes; a 409 is confirmed against a fresh read before showing the intended state. Failure retains exploration results and offers read-only reconciliation. GET /poi-favorites now includes the existing joined Google place ID as `poiPlaceId`; heart identity no longer conflates same-name businesses. No database schema or authorization policy change.
- Region selection reuses TripSelect's listbox and immediately applies the selected search scope. The category overflow reuses the installed Headless UI menu for arrow keys, Escape and focus return. Inline category filters are native pressed buttons in a labelled group, rather than tabs without panels. Existing card/filter arrangement remains; the controls replace broken keyboard behavior without a new page layout.
- Verification: 14 unit/page files / 96 tests, 2 real-D1 API files / 17 tests, and 2 Chromium flows = 115 checks. Browser coverage includes mobile pagination failure/retry/deduplication, failed add then success with context retained, and desktop region/overflow keyboard and Escape. Frontend and Functions typechecks, targeted lint, browser build and diff check pass. Logs: `t21-unit.log`, `t21-api.log`, `t21-e2e.log`, `t21-types.log`, `t21-functions-types.log`, `t21-lint.log` under `/tmp/tripline-1307-checks/`. Some legacy DOM tests log jsdom's unimplemented window.scrollTo; tests pass and the corresponding browser navigation flows pass.
- Standards review pinned at `94de08e8`: search coordination is removed from the large JSX page into its own actual paginated-results owner; no common search abstraction erases the distinct contract. Existing request, coordinate, region, picker, menu and entry-mutation modules are reused. Canonical identity is derived from the real backend read shape. Existing UI tests now open the accessible overflow and assert pressed filter semantics. No outstanding standards finding.
- Spec review against published T21: zero results versus failed reads, retained load-more results/query, stale-query exclusion, duplicate favorites, independent mutation feedback, add failure and return context, and region/category keyboard behavior are verified through the real pages with HTTP effects replaced. Whole-candidate checks and the UAT PR remain required; issue stays open pending delivery audit.


## Favorites T22 verification and review

- Red evidence (`t22-red.log`) reproduces visible-only selection replacing hidden selections, successful rows remaining until the entire batch/refetch finishes, and a prior refresh resurrecting an acknowledged removal. The actual page tests keep HTTP as the only mutation/read replacement.
- The existing favorites page module continues to own filters, page and identity-based selection. “全選本頁” adds the displayed rows to the existing selection. The toolbar identifies how many selected rows are outside the current page; the existing confirmation modal freezes the intended rows and lists their names/addresses in a bounded scroll region. Filtering never silently changes the pending operation's meaning.
- Each DELETE acknowledgement removes only that row and its selection. Explicit removed/failed result sets remain visible, failures remain selected, and retry submits only remaining IDs. A synchronous busy guard rejects duplicate confirmation and selection changes during a batch. There is no success-dependent refetch and no optimistic removal. Read generations invalidate refreshes begun before a mutation, read results are validated and reconcile selection against actual server membership, and late completions cannot update an unmounted visit. The existing soft-delete endpoint and authorization contracts are unchanged.
- Pagination clamps to a valid populated page after removal and exposes the current page beyond the first eight numbered buttons. The existing bottom toolbar follows the result grid, so its sticky-bottom positioning remains reachable while browsing a long list. Modal focus uses the existing fallback when the opener becomes disabled or disappears. Existing pressed filter semantics and per-card-only add-to-trip policy remain intact.
- Verification: 7 unit/page files / 38 tests plus 2 Chromium flows = 40 checks. These cover cross-filter/page selection, displayed-page select-all, named confirmation, no removal before server response, independent acknowledgements, partial failure, duplicate confirmation, failed-only retry, stale refresh, final-page clamping, safe initial modal focus and reachable bottom actions. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: `t22-unit.log`, `t22-e2e.log`, `t22-types.log`, `t22-lint.log` under `/tmp/tripline-1307-checks/`.
- Zoom/reflow verification uses the 320×450 layout viewport corresponding to a 640×900 desktop at 200 percent zoom; this is viewport emulation, not a native browser zoom command. An initial CSS zoom experiment was unsuitable because it scaled the viewport-locked shell without changing its CSS viewport, so it was replaced with the explicit layout-viewport check. Both final browser scenarios pass. Some unchanged legacy DOM tests log jsdom's unimplemented window.scrollTo; browser focus/navigation checks pass.
- Standards review pinned at `7a4b14de`: operation/result ownership stays with the existing module rather than creating a second collection or mutation framework. Existing request, confirmation, refresh and focus modules are reused. The explicit result set and identity guards replace the former all-or-nothing refresh/reset behavior. No outstanding standards finding.
- Spec review against published T22: operation membership is reviewable across filters, success is reflected per item, failed rows survive and can be retried without repeating successful DELETEs, and server confirmation precedes removal. No new persistent contract or layout is introduced; toolbar placement and confirmation detail repair the existing batch workflow. Whole-candidate checks and the UAT PR remain delivery gates; issue stays open pending final audit.


## Maps T23 verification and review

- Reproduced malformed accessible summaries being accepted as an empty list (or throwing while resolving selection), overview pin selection being overwritten by the first card of the selected day, Google place cards surviving a day change, and missing coordinates/read failure appearing as an empty itinerary. Root-map recovery now reuses the existing account-scoped list owner, which validates summary identity/display fields and rejects duplicate identities; retry shares pending work and cannot target a later account. Failed reads retain preference and last-known data. No new list store or selection abstraction.
- The URL owns the active day. An explicit page selection owns the entry within that day, with fallback derived from available pins; no reset effect competes with a click. Trip/entry route visits are keyed, stale card observers are invalidated, and one cleaned-up scroll timer replaces the unowned click timeout. Selecting a pin, selecting a card and switching dates use that same state. A day change dismisses an external Google place card, including browser navigation.
- Trip read errors and missing-coordinate entries are distinguished from confirmed empty data. A persistent link opens the selected itinerary list even if Google Maps cannot load; root-list errors offer retry and the trip-list entry. Error copy is announced. Existing Google Maps loading, provider, attribution, route rendering and read-only behavior remain intact.
- Browser hit testing reproduced attribution hidden under the bottom navigation. The existing canvas now reserves navigation/safe-area space; cards leave the attribution strip and control column clear. On short viewports, the existing two FABs form a row and the card region is bounded and scrollable, preserving the focused point and zoom controls. These are overlap/reflow bug fixes to existing controls, with no new page or component. A temporary minimum-height attempt was removed after browser evidence showed it still put attribution under the fixed navigation.
- Verification: 24 unit/page/module files / 200 tests, plus 5 Chromium flows covering 320x450, 390x844 and 1280x900, pointer/Enter zoom, pin/card/day alignment, long labels, attribution/control hit targets, legacy day switching and read failure. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: `t23-red.log`, `t23-unit.log`, `t23-e2e.log`, `t23-types.log`, `t23-lint.log` under `/tmp/tripline-1307-checks/`. Final alert-role addition is covered by the targeted real-page regression rerun.
- SDK scope: actual pages, routing, trip reads, map hooks and selection implementations run in these checks. HTTP and Google SDK effects are replaced at their existing external seams. Browser geometry validates application overlays against provider-control/attribution test doubles; it is not evidence of live Google authentication, tiles or provider rendering. Existing backend read/authorization contracts are unchanged, so no new D1 contract test is required for this ticket.
- Standards review pinned at `243ffef9`: reused the existing list and map owners, removed competing derived state and an unowned timer, inspected all current list consumers and the actual `/my-trips` response shape. No additional dependency or speculative adapter. Spec review: selection continuity, failed-read preference retention/retry, failure-versus-empty copy, list fallback, pin/card/day agreement and operable visible map chrome are covered. No outstanding review finding; whole-candidate verification and the UAT PR remain required. Issue stays open pending final delivery audit.


## Timeline T24 verification and review

- Reproduced lost reorder result/error state and focus: DaySection keyed its entire content for animation, destroying the rail on activation; day invalidation also deleted from the rendered snapshot. The existing animation now toggles a CSS class without remounting, and useTrip invalidates a copied lookup cache. Reorder and cross-day drag retain explicit saving, saved-with-traffic-pending, completed and failed results. Duplicate writes are guarded; traffic retry uses the accepted mutation result and never resubmits the entry write.
- Menu and drag continue through the existing entry mutation verbs. A successful move focuses the target entry after its destination data arrives; move/copy form return links identify the accepted entry (including the new copy ID). TripPage derives the owning day from authoritative entries, expands and focuses the target once per visit, and ignores invalid focus parameters. Browser checks confirm the target is in view. Error results preserve the original order and source focus. Retry controls transfer focus when they disappear, not after an asynchronous result that could steal focus.
- Overlapping accepted writes previously shared a recompute that started before the newer write. The existing travelRecompute module now supports afterWrite freshness: wait for the old flight, regardless of its success, then start or join the next fresh flight. The entry mutation owner applies this to every accepted entry verb; failed-only retries retain their existing policy. No new mutation framework or dependency. The real-page regression exercises both old-flight success and failure before the latest order can report completion.
- Operation generations protect trip switches and attempts protect superseded order results. A final red regression exposed the cross-day retry guard surviving a trip switch; it now belongs to the attempt and is reset with the trip lifetime. A pending retry from A neither blocks B's retry nor overwrites B's result when it finishes.
- Verification: 28 unit/page/module files / 231 tests, followed by the expanded 38-case synchronization file (two additional cases: 233 distinct unit/page checks); existing real-D1 batch handler / 10 tests; 3 Chromium flows for mobile keyboard ordering/day switching/retry, desktop non-drag destination focus, and readonly rejection. Frontend typecheck, targeted lint, browser build and diff check pass. Logs: t24-unit.log, t24-final-synchronization.log, t24-api.log, t24-e2e.log, t24-types.log, t24-lint.log under /tmp/tripline-1307-checks/. Missing local Playwright cache initially prevented launch; reinstalling only the matching Chromium headless shell restored functional verification. No dependency or lockfile changed.
- Standards review pinned at 86409618: existing entry, segment, day-read and page owners retain their responsibilities; callers do not acquire a second write policy. Removed obsolete source-level focus assertions only after real-route coverage replaced them. Legacy mock-based characterization expectations were updated for the existing module's freshness option; new coverage retains real domain implementations and replaces HTTP/geometry only. No outstanding standards finding.
- Spec review against T24: non-drag and keyboard controls, day/trip lifetime, optimistic suppression, partial-save recovery, readonly rejection and return focus are covered. These are recovery, lifecycle and accessibility repairs to the existing layout, with no new page/component or persistence policy. Whole-candidate checks and the UAT PR remain required; issue stays open pending final delivery audit.
