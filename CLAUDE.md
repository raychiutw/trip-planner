# Tripline

Cloudflare Pages + D1 + React SPA + V2 OAuth. Admin: lean.lean@gmail.com.

## Pipeline

`Think → Plan → Build → Review → Test → Ship → Reflect`

- Think `/office-hours` · Plan `/autoplan` · Build code + `/simplify`
- Review `/tp-code-verify` + `/review` (mandatory)
- Test `/cso --diff` (mandatory) + `/qa`
- Ship `/ship` → `/land-and-deploy` → `/canary` · Reflect `/retro`

## Hard Rules

- **Code change → invoke `/tp-team` first** (新功能、bug fix、refactor、migration、CSS、API endpoint)。行程資料用 `tp-*` data skills 直接打 API。
- **Mockup-first hard gate**：所有 new page / new component（≥1 layout 變化）→ `/tp-claude-design` 產 HTML mockup → user sign-off → 才寫 React。Bug fix / token drift / 純 prop tweak 例外。
- Feature branch + PR via `/ship`. Never push master directly.
- `tp-*` skills hit API, not local files.
- Agent tool only for worktree isolation.
- Web browse: `/browse` only, never `mcp__claude-in-chrome__*`.
- Post-ship retroactive OpenSpec archive if PR didn't propose first.

## Layout

`src/` SPA · `functions/api/` Pages Functions · `migrations/` D1 · `tests/` · `css/tokens.css` Tailwind 4.

Desktop ≥1024px: 2-col timeline + sticky map. Mobile: 5-tab nav.

## Dev

```bash
npm run dev:init   # local SQLite
npm run dev        # vite 5173 + wrangler 8788
```

Mock auth: copy `.dev.vars.example` → `.dev.vars` (NOT `.env.local`), set `DEV_MOCK_EMAIL`.
Prod `TRIPLINE_API_URL`: funnel listens `:443`, not `:8443`.

## Design SoT

`DESIGN.md` + `docs/design-sessions/terracotta-preview-v2.html` are UI/UX truth. Code mismatch = bug. Conflict → discuss first.

## Skill Routing

Match → invoke `Skill` first.

- Brainstorm → `/office-hours` · Bug → `/investigate`
- Ship/PR → `/ship` · QA → `/qa` · Code review → `/review`
- Doc sync post-ship → `/document-release`
- Visual → `/design-review` · Architecture → `/plan-eng-review`
- Browse → `/browse`

Detail: `ARCHITECTURE.md`, `GEMINI.md`, `DESIGN.md`, `.claude/skills/tp-team/SKILL.md`.
Prod: https://trip-planner-dby.pages.dev/ · GBrain: pglite + MCP (user scope), sync=full, repo=read-write, 873 pages, setup 2026-05-04. Windows caveat: transcript ingest no-op (script POSIX-only). See `~/.gbrain/config.json`.

## Naming history

- **v2.36.0–2.38.1 (行程列印/匯出/匯入 redesign，office-hours milestone)**: 三件需求（列印頁有收合不適合列印 / 匯出要同步 / 匯入 JSON 要做）一條資料路徑解。office-hours 設計（Approach A 資料驅動列印文件，9/10）→ mockup 簽核 Variant A「緊湊表格式」→ 3 PR 走 tp-team pipeline（每輪 adversarial review + security audit）。**v2.36.0 (#890) 列印文件頁**：新 route `/trip/:id/print`（`TripPrintPage` + `TripPrintDocument` + `lib/tripPrintData.ts`），從 data render（不再吃互動 TripPage DOM）→ 列印/PDF 永不繼承 accordion/collapse；reuse 既有 `mapDay.toTimelineEntry`（time 由 startTime/endTime 組、title 是 poiName??title、rating 在 master stopPoi、travel.type 是 car/walk 不是 driving/walking — code-review 4 個 data-mapping blocker 全修 + loader test 鎖）；含行程筆記 5 區塊、A4 多頁、0 天 placeholder、`title||name` fallback；列印選單 repoint 到新頁（含 e2e fix）。**v2.37.0 (#891) 匯出同步**：**移除 CSV+Markdown**（不再使用）— 刪 `csvSafe`/md/csv branch/`DOC_LABEL_MAP`；PDF 改 `renderTripPrintPdf`（component 層，off-screen 附著容器 + `flushSync` + html2pdf，不再吃 `#tripContent`；30s timeout + in-flight guard + windowHeight）；JSON round-trip schema `{schemaVersion:1, meta, days(+entryPosition), notes(5 區塊), segments(positional fromEntryIdx/toEntryIdx, orphan dropped)}` 取代舊 `{meta,days,docs}`（breaking）；分層：`lib`（tripExport/tripPrintStyles）leaf 不 import component。adversarial review 8 finding 全修。**v2.38.0 (#892) 匯入 JSON**：`POST /api/trips/import` + 列表 TitleBar「匯入」button（新 upload icon）→ 建新行程（永不覆寫，owner=當前 user，data_source='imported'）。安全邊界 `functions/api/trips/_import.ts`（純函式）：實際 512KB（讀 text 非信 Content-Length）+ schemaVersion gate + 拒 `__proto__`/`constructor`/`prototype`（含 non-enum/symbol）+ 每陣列上限 + **總數上限**（防 D1 batch 爆量）+ 每 CHECK enum coerce（poi type/seg mode+source/resv+emergency kind）+ poi sort_order 重編 1..N + segment (from,to) 去重 + allowlist 讀欄位（不 spread）。orchestration：D1 無 transaction + batch ~100 上限 → **chunked sequential batch（≤50）+ INSERT…RETURNING id + 逐 chunk track id + connect-root rollback**；每人行程數上限。security review 2-dim → adversarial verify，**10 finding 全修**（CRITICAL batch blowout、Content-Length bypass、UNIQUE violations、trip cap）。**v2.38.1 (#893) prod bug 修**：prod 實測（沖繩真實行程）發現含 POI 匯入全 503 — `pois` 有 `UNIQUE(name,type)` index（migration 0018/0025），裸 `INSERT INTO pois` 撞既有 POI 整筆 fail → rollback；非單元測試可測（無 D1 binding），靠 live 驗證才抓到。改 `resolvePoi` **find-or-create by (name,type)**（SELECT 既有沿用 AS-IS 不 mutate / 否則 INSERT OR IGNORE + re-fetch；只 track 新建 id rollback）+ per-entry poi_id 去重（UNIQUE(entry_id,poi_id)）+ `MAX_TOTAL_POIS` 3000→400（sequential 守 subrequest budget）。Prod round-trip 驗證：匯出沖繩 143KB JSON → 匯入 201 → **5 天/37 景點/85 POI/4 住宿/32 交通/筆記 完全還原**。教訓：任何寫 pois 的功能必 find-or-create（見 `functions/api/_poi.ts`）；列印/匯出 loader 必 reuse `mapDay.toTimelineEntry`；lib 是 leaf 不 import component；下載類 prod 驗證用 `URL.createObjectURL` override 攔 blob。
- **v2.35.0 (行程筆記 milestone) + v2.34.47-50 prod QA**: 行程筆記功能（`/trip/:id/notes`，5 section 航班/住宿/預訂/行前須知/緊急聯絡 + AI 代寫行前須知/緊急聯絡 + per-section CRUD + 拖曳排序 + autosave-on-blur）的 2.34.0–2.34.50 開發收斂為 **2.35.0** minor milestone（純版號 + CHANGELOG，無 code 變更，PR #888）。本輪 prod QA 4 連修（mobile 截圖驅動）：**v2.34.47 (#884)** `TripNotesPage.tsx:218` tripName `tripCtx?.trip?.title ?? null` 的 `??` 接不到空字串 → destination-named 行程（`title=''`，靠 `name` 顯示）筆記頁 TitleBar 缺行程名 + 空 hero eyebrow 空白，改 canonical `title || name` fallback（對齊 TripsListPage 卡片 `t.title || t.name`）；**v2.34.48 (#885)** 5 section 編輯表單原本用原生 `<input>`/`<select>`/`<input type=datetime-local>` + 各自散落 `.tp-notes-*-edit-grid input` ad-hoc CSS（**無 min-height**）→ 原生 date/time 比文字欄高、同列高度不齊 + 原生瀏覽器 chrome，全改 canonical：文字/textarea → `.tp-input-long`、native `<select>` → `TripSelect`、native `datetime-local` → 新 `src/components/trip-notes/NoteDateTimeField.tsx`（組合 `TripDatePicker` + `TripTimePicker`，split/combine helper + local state 避免 autosave round-trip 丟失另一半）；**v2.34.49 (#886)** NoteDateTimeField time picker 擠在 92px 窄欄 → 22px 粗體「--:--」placeholder 在連字號斷行 → 觸發器爆高到 125px（vs date 59px），改 time 欄 128px + `white-space: nowrap` + 欄位 max-width 460；**v2.34.50 (#887)** 編輯模式只有「刪除」沒有退出鍵 → 5 section 加 ghost「關閉」button（`onClose → setEditingId(null)` 收合不刪，autosave 已存）+ 手機左右滑動 root cause `.tp-notes-shell` 是 CSS grid 但缺 `grid-template-columns` → 隱式 `auto` column blowout 到 content max-content → `.tp-notes-page-body` 413px > 390 viewport → `app-shell-main` overflow-x:auto 橫向捲動，加 `grid-template-columns: minmax(0, 1fr)` 鎖 column 上限（live 驗證 413→390）。每修 3050 test 全綠 + tsc 0 err + regression test 鎖（canonical 元件接線 / 無原生 date-time-select / nowrap / 關閉 button × 5 section / shell column cap），3 round prod mobile 驗證（高度一致 + 客製 popover 0 原生 + 無 hscroll）。
- **v2.33.121-122**: display_name lifecycle UX — prod QA (rayschiu@fetci.com `display_name=null`) 發現 sidebar 顯整 email「rayschiu@f...」(整 email truncated) 但 `/account` hero 顯「rayschiu」(local-part) — 兩處 fallback chain canonical 不齊。v2.33.121 fix：`DesktopSidebarConnected.tsx:29` 從 `displayName ?? email` (2 層) 對齊 `AccountPage:215` + `ChatPage:881` canonical 3-層 `displayName ?? email.split('@')[0] ?? email`。v2.33.122 follow-up：補 user 主動設定 display_name 的 UI（之前只能 signup「名稱（選填）」一次決定，之後無 UI）。新 backend `PATCH /api/account/profile` endpoint（trim + max 50 chars + empty 視同 null clear + UPDATE users + audit log + response mirror `/api/oauth/userinfo` shape）+ AccountPage hero name 旁 ✏ pencil edit button → modal（input + 取消/儲存 + Enter/ESC 鍵盤 a11y + 儲存成功 `reloadUser()` + toast）。Modal placeholder 顯 email local-part 暗示 fallback 行為。共 18 條 regression test。
- **v2.33.118-119**: AI 健檢 page CTA state-based redesign — prod QA dark mode 截圖顯示 titlebar 右上 sparkle icon-only `TitleBarPrimaryAction is-primary` 在 dark mode 下 brown accent fill 與 chrome 落差大 + icon 沒 label 看不出 action 意圖。Redesign 按 state 拆 CTA：empty/idle 拔 titlebar action 改 body 中央 accent-pill「開始 AI 健檢」(C variant)；pending/completed/failed 用 ghost icon button (`.tp-titlebar-action` 無 `.is-primary`) + lucide `refresh-cw` icon（取代 sparkle，「重新生成」語意明確）；pending 加 spin animation + `prefers-reduced-motion` 關動畫保 a11y。v2.33.119 follow-up 拔掉 findings count badge — 在頁面內重複（meta + list 已顯）+ 跨頁入口 (TripCardMenu / trip card) 也沒帶 → 孤兒。新 9 條 regression test 鎖設計（icon registry / titlebar conditional / ghost style / refresh-cw / spin / body CTA / pill style / reduced-motion）+ 既有 `trip-health-check-empty-guard.test.ts` `button disabled 條件` assertion 對齊（CTA 拆 2 button，entryCount guard 搬到 body CTA）。
- **v2.33.117**: 未登入 page（login/signup/forgot/verify/reset）不認 dark mode 設定 / 系統 prefers-color-scheme — FOUC。`<DarkModeInit />` 雖 v2.31.25 已 mount 在 Routes 外但 `useDarkMode` useEffect 加 `body.dark` class 必須等 React commit phase fire，第一個 paint 仍 light。Fix：`index.html` `<body>` 開頭加 inline blocking script 同步讀 `tp-color-mode` localStorage + 檢查 prefers-color-scheme + 加 `body.dark` + 更新 `<meta name="theme-color">`，在 React mount **之前**完成 → 第一個 paint 就是正確主題。Script mirror `src/lib/localStorage.ts` LsEntry shape + `useDarkMode.readColorMode` 三向 mode + legacy `tp-dark` boolean key fallback。順手修 `VerifyEmailPage.tsx` 用 undefined token `--color-bg` / `--color-paper` → `--color-secondary` / `--color-background` 對齊其他 auth page 標準 token（之前 fallback transparent，dark mode 偶然「正常」實則 body bg 穿透無 contrast）。7 條 regression test。
- **v2.33.114**: VerifyEmailPage 驗證 token 被企業 email scanner pre-consume — user 點信件連結看到「已使用」誤導訊息。QA 2026-05-25 rayschiu@fetci.com signup 復現：signup 後 17s `email_verified_at` 已被寫 timestamp（user 還沒收到信）。Root cause：v2.33.59 設計用 `useEffect` mount 時 auto-POST `/api/oauth/verify` 假設「Email client image-preview 不會跑 JS」，但 enterprise email security（Mimecast / Microsoft Safe Links / Proofpoint URL Sandbox）跑 headless Chromium deep inspection — render page → JS 執行 → useEffect 觸發 → silent consume token。Fix：拔 `useEffect` auto-POST，idle state 顯「點此完成驗證」button → user click 才 POST。Scanner headless render 不會自動 click button（會被 reCAPTCHA / behavior detection trigger，scanner 規避）。`missing_token` error 改在 `useState` initializer derive（避免 render 時 setState anti-pattern）。2 conditions regression test：grep 鎖 `useEffect(...performVerify)` 不存在 + button testid。Prod QA 復現 fix：97s 後 `email_verified_at` 仍 NULL（對比 v2.33.59 的 +17s 自動 consume）。
- **v2.33.111-113**: 3-layer infra fix — cleanupOrphans + /trigger timeout — AI 健檢 request 209 在 prod 卡 1h21m 才完成，Telegram 噪音 alert。Root cause 三層獨立 failure：(1) `scripts/tripline-api-server.ts:92` `SESSION_PREFIX = 'tripline-request-'` 在 v2.33.27 per-skill rename 後永遠 match 不到 `tripline-tp-*-*` session → orphan 完全不被清 → `hasActiveSession()` 永真 → cron 每 10 min skip；(2) CF Worker `fetch(TRIPLINE_API_URL + '/trigger')` `AbortController` 3s timeout 對 CF Edge → Tailscale Funnel cold connection（DNS + TCP + TLS + 偶爾 DERP relay）4-5s 必 abort；(3) funnel drift 第三次發生（GUI/brew/macOS update 改 `serve` tailnet only）。Fix layers：v2.33.111 `cleanupOrphans` 用 `getKnownSessionPrefixes()` 從 ALLOWED_SKILLS derive + LEGACY allowlist-driven 比對；tmux ls format 從 space-delimited 改 `|`-delimited 防 session name 含空格的同病灶 race（adversarial review CRITICAL finding）；`hasActiveSession` 簽名改 required，刪 SESSION_PREFIX 死碼。v2.33.113 CF AbortController 3s → 8s 涵蓋 99% cold path。Funnel drift 手動 reset（`tailscale serve reset` + `tailscale funnel --bg --https=443 http://127.0.0.1:8080`）+ 留 P1 funnel-guard launchd 護衛 TODO 治本。9 + 4 regression test。
- **v2.31.16**: EntryActionPage `/copy` / `/move` day picker 永遠顯「空」即使 day 有 stops — fetch `/api/trips/:id/days`（no `?all=1`）讀 `d.entryCount`，但 backend 該端點不回此欄位 → 永遠 undefined → fallback 0 → UI 顯「空」。Fix：改用 `/days?all=1` endpoint（已存在）+ `Array.isArray(d.timeline) ? d.timeline.length : 0`。3 個 regression test。
- **v2.31.15**: EditTripPage「預設交通方式」永遠顯示「自駕」— TripApi 仍含 snake_case `default_travel_mode` / `data_source` read path，backend response 是 camelCase `defaultTravelMode` / `dataSource` → 讀取永遠 undefined → fallback `'driving'`。Driving trip 因 fallback 巧合正確 mask 此 bug；walking/transit trip user 看到錯誤 default。同 #573 / #574 camelCase 對齊 bug 家族。Fix：type + read camelCase；write 維持 snake_case (backend `WRITABLE_FIELDS` allow-list 是 snake)。3 個 regression test。
- **v2.31.14**: AI 健檢 finding 的「前往景點」/「前往 Day」按鈕永不 render — 同 #573 camelCase 對齊 bug 家族。`TripHealthCheckPage.Finding` 寫 `action_target?: {day?, entry_id?}` snake_case + 6 處 reference 都 snake；backend 經 `deepCamel` 回 `actionTarget: {day, entryId}` camel → `f.action_target?.entry_id` 永遠 undefined → button 永不 render。Fix：type + 6 處改 camelCase。順手把 `trip-health-check-page.test.tsx` mock data 也改 camelCase（之前 mock snake 跟真實 API shape 不一致，假性 green 蓋住此 bug）。新 4 個 regression test。
- **v2.31.27**: chat AI 健檢 user message 顯短摘要 — Bug #128（prod QA, v2.31.18 follow-up）。`trip_requests.message` 寫整個 HEALTH_CHECK_MESSAGE system prompt（含 5 維度 + JSON schema + 範例）→ chat 直接 render let user 看一大坨雜訊。v2.31.18 fix reply 沒 fix message。Fix：`src/pages/ChatPage.tsx` `buildPairsFromRequest` 偵測 `row.message.startsWith('[AI 健檢]')` → displayText 改「已觸發 AI 行程健檢」。完整 prompt 仍存 trip_requests.message → api-server 送 Claude（backend 邏輯不變）。4 個 source-grep test。
- **v2.31.26**: sanitizeHtml 允許 SPA 相對路徑 href — Bug #127（prod QA, v2.31.18 follow-up）。v2.31.18 AI 健檢 reply 含 markdown link `[前往健檢報告](/trip/:id/health)`，prod 顯示文字但 `<a>` 沒 href（不能 click）。Root cause：`src/lib/sanitize.ts` line 44 regex `/^(https?:|tel:|mailto:|#)/` 不允許相對 `/path`。Fix：加 `|\/(?!\/)` 允許 `/path` 但拒 protocol-relative `//host` security 漏洞。8 個 unit test。
- **v2.31.25**: dark mode 跨 page 沒套用 body.dark class — Bug #126（prod QA）。user 切「深」mode 後 `tp-color-mode` localStorage 正確存「dark」，但切去 /trips /chat /favorites /explore 等 page 後 `body.dark` class 沒套用 → page bg 還是 light。`useDarkMode` hook 之前只在 ThemeToggle / TripPage / GlobalMapPage 三處 mount，其他 page 無 init body.dark。Fix：加 root-level `<DarkModeInit />` component（`useDarkMode()` + return null）mount 在 main.tsx `<BrowserRouter>` 下 `<Routes>` 外。3 個 source-grep test。
- **v2.31.24**: 日本 24h POI 顯示日文「24時間」→ 中文「24 小時」— Bug #125（prod QA）。那霸機場 timeline expand 顯「★ 4.1 · 24時間」(日文 raw from Google Places)。Fix：`src/lib/poiHours.ts` `condenseHours` 加 special case `/^24\s*時間(?:営業)?$/` → '24 小時'。涵蓋 `24時間` / `24 時間` / `24時間営業` 三種形式。4 個 unit test (3 轉換 + 1 中文保留 regression)。
- **v2.31.23**: transport POI label 統一為「交通」— Bug #124（prod QA）。`timelineUtils.deriveTypeMeta` line 140 對 `poiType === 'transport'` 返「移動」，但其他 4 處 POI_TYPE_LABEL（poiCategory.ts / TimelineRail / EditEntryPage / lib）都返「交通」造成不一致：TripPage TimelineRail「那霸機場 移動」vs EditEntryPage「那霸機場 交通」。Fix：line 140 改「交通」對齊 canonical。line 157 (text-based「開車/drive」keyword) 仍返「移動」（描述 segment 行為，保留）。4 個 vitest unit test（call helper 直接驗 return value，非 source-grep）。
- **v2.31.22**: ExplorePage category filter 0 結果補 empty state — Bug #123（prod QA）。搜尋「拉麵」+ 切「景點」filter → 0 個結果 + grid 空白讓 user 迷路。Fix：`filtered.length === 0` 時 conditional render「沒有符合「{CATEGORY_LABELS[category]}」的結果」+「回到為你推薦」reset button onClick `setCategory('all')`。CATEGORY_LABELS map（all/attraction/food/hotel/shopping → 為你推薦/景點/美食/住宿/購物）+ CSS dashed placeholder + accent pill button。4 個 source-grep test。
- **v2.31.21**: AddPoiFavoriteToTrip 結束時間 helper text typo — Bug #122（prod QA）。「可空 — 依停留時間預估推」缺字（line 557），對齊 line 544 開始時間「可空 — 依景點類型自動推算」改為「可空 — 依停留時間自動推算」。3 個 source-grep test（新文案 + 殘缺不再出現 + 開始時間 regression）。
- **v2.31.20**: ExplorePage 搜尋結果 card 顯示中文 type label — Bug #121（prod QA）。`{poi.category || 'POI'}` 直接 dump Google Places primary type raw enum（RAMEN_RESTAURANT / RESTAURANT），中文 user 無意義且不一致（同類別出 2 種 enum）。Fix：import POI_TYPE_LABELS + 走 `POI_TYPE_LABELS[mapNominatimCategory(poi.category)] ?? 'POI'`，把 Google Places 細分 type → Tripline whitelist → 中文 label。Helper 已存在 src/lib/poiCategory.ts，ExplorePage 漏 import + 漏 call（AddStopPage / ChangePoiPage 都已用）。4 個 source-grep test。
- **v2.31.19**: PoiFavoritesPage 主收藏頁 card 補回 ★ rating — Bug #120（prod QA）。v2.31.17 補了 backend SELECT + AddStop/ChangePoi favorites card，主頁 `/favorites` 漏掉。Fix：`PoiFavoriteRow` 加 `poiRating?: number | null` + card body 加 `★ N.N · address` conditional + CSS `.poi-rating`（accent weight 600）+ `.poi-meta-sep`。4 個 source-grep test。
- **v2.31.18**: AI 健檢 reply 在 chat 顯示為 raw JSON array — Bug #115（prod QA）。Claude 回純 JSON array（per HEALTH_CHECK_MESSAGE prompt 設計），`applyHealthCheckCompletion` parse 進 `trip_health_reports.findings_json` 但忘記改 `trip_requests.reply`，chat UI 把 reply 當 markdown 直接渲染 raw JSON。Fix：backend hook 完成 / 失敗兩條 branch 同時 UPDATE `trip_requests.reply` 為 user-friendly summary（`AI 健檢完成 — 發現 N 個 finding（high H · medium M · low L）。\n\n[前往健檢報告 →](/trip/:id/health)`），raw findings 仍存 `trip_health_reports.findings_json`。新 helper `buildHealthCheckSummary` + `rewriteRequestReply`。5 個 source-grep unit test（完成 / 失敗 branch / summary 內容 / helper signature）。
- **v2.31.17**: poi-favorites GET SELECT 補 `p.rating AS poi_rating` + AddStopPage / ChangePoiPage favorites card 補回 ★ rating 顯示 — Bug #114（prod QA）。v2.31.10/11 因 backend SELECT 缺 rating，孤兒 star icon 誤導 user，當時先拔；本版補回 backend data flow + frontend `PoiFavorite.poiRating?: number | null` + `normalizePoiFavorites` camelCase + snake_case fallback + favorites card `★ N.N · address` conditional（rating 不存在則只顯 address）。3 個 unit test（poi-favorites-select-rating 新檔 + add-stop / change-poi 既有 test 反轉 expectation 從「不該有 star」改「rating 存在才 render star」）。
- **v2.31.13**: EditTripPage destinations 沒 load — 顯示「尚無目的地」。Backend GET `/api/trips/:id` 經 `deepCamel` 回 `destinations: [{destOrder, name, lat, lng, dayQuota, subAreas}]` camelCase；EditTripPage `TripDestApi` type 寫死 snake_case + filter 用 `typeof d.place_id === 'number'` 永遠 false（`trip_destinations` 表沒 place_id 欄位）→ 全 filter 掉。Fix：type 對齊 camelCase + filter 改 name-based valid（對齊 backend `isValidDestination`）+ 既有 dest 用 synthetic `existing-${destOrder}-${name}` 當 React key。
- **v2.31.12**: ExplorePage POI 卡片 rating 寫死 placeholder「探索更多評論」，comment 寫「真實 rating 待 backend 提供」是 v2.23.0 google-maps-migration 前的 stale comment。Backend 早就回 `PoiSearchResult.rating: 4.6`，UI fix 為 `{typeof poi.rating === 'number' ? poi.rating.toFixed(1) : '探索更多評論'}`。Fallback 仍保留 placeholder 字串（無 rating data 時）。
- **v2.31.11**: ChangePoiPage 同樣 3 個 search rating + section title + favorites star bug — copy-paste from AddStopPage 修法。Section title `query.trim().length >= 2 ? '搜尋結果' : '熱門景點'`，search card 條件 render `★ {rating.toFixed(1)} · address`（ChangePoiPage `normalizeSearchResults` 用 cast，rating 已含），favorites card 暫拔孤兒 star（待 task #114 backend SELECT 補）。3 個 regression test。
- **v2.31.10**: add-stop page 三個 prod QA 小 bug — (1) section title 寫死「熱門景點 · {region}」搜尋態語意不對，改成 `query.trim().length >= 2 ? '搜尋結果' : '熱門景點'`；(2) search card star icon 沒值（backend 有回 `rating: 4.6` 但 `normalizeSearchResults` 沒抽），補 `typeof item.rating === 'number'` extract + card meta 條件 render `★ {rating.toFixed(1)} · address`；(3) favorites card 同樣孤兒 star（poi-favorites API SELECT 沒拿 rating，schema 有 pois.rating），先拔 star avoid 誤導，#114 follow-up backend SELECT 補。
- **v2.31.9**: `useTripSegments` N+1 fix — 5-day trip 詳細頁原本平行 5 個 `GET /api/trips/:id/segments`（每個 `TimelineRail` 自己 call hook）。新 `TripSegmentsContext` provider 在 `TripPage` 一次 fetch，children TimelineRail 透過 context 共用 `segmentMap`。Hook 內部 `useContext(TripSegmentsContext)` 偵測 provider 存在 → 直接 return context 值；缺席（EditEntryPage 等獨立頁面）→ 退回原本 fetch path。3 個 regression test：context 存在不 fetch、多 hook caller 共用、context 缺席仍正常 fetch。
- **v2.31.8**: 兩個 v2.31.7 prod QA 補丁 — (1) TravelPill 初始 render 閃顯反向方向：v2.29.0 backend rewrite 後 `entry.travel = segmentsMap.get(from_entry_id=eid)` 語意是「離開此 entry」，但 UI pill 位於 (prev → curr) 中間應讀「離開 prev = 抵達 curr」=`prev.travel`。`TimelineRail.tsx` fallback 從 `entry.travel` 改 `prev?.travel`，segments 載入後 segment prop 接手覆蓋仍正確；更新 `timeline-rail-segments-wiring.test.tsx` 既有 fallback test 對齊 backend semantic（travel 改掛 prev = entry 1）+ 新增 regression test 確認 travel on curr 不誤觸發 fallback。(2) AI 健檢「30 秒內完成」誤導：實測 request #190 跑 3m46s、#196 跑 9 分鐘、#187 worst case 1h19m；empty / pending state 文案改「3-7 分鐘完成」。
- **v2.31.7**: D1 naive datetime UTC parse 修正 — D1 `datetime('now')` 回 `YYYY-MM-DD HH:MM:SS` 無 Z 後綴，前端 `new Date(s)` 當 local time → 顯示落差 TZ offset 小時（AI 健檢「8 小時前完成」實際 7 分鐘）。新 helper `src/lib/parseUtcDate.ts` 偵測 D1 naive datetime 補 Z 後 UTC parse。修 4 個 callsite：TripHealthCheckPage formatTimestamp、ChatPage formatChatTime+formatDayDivider+buildMessagesWithDividers、SessionsPage relativeTime、DeveloperAppsPage created_at。
- **v2.31.6**: Chat polling robust 化 — 解 SSE 逾時 silent 卡死。Server `MAX_DURATION_MS` 10 min → 30 min（request #187 曾跑 1h19m）；client `useRequestSSE` 重寫 — polling 永遠跑（safety-net 30s interval），SSE 退位成 latency optimization。原本 EventSource auto-reconnect 不一定觸 `onerror`，clean-close 後 polling fallback 永遠沒啟動。新增 `errorReason='auth_expired'`（401 polling → 登入過期提示）+ `elapsedMs` UI 顯示「AI 還在處理（已等候 N 分鐘）」避免 spinner-only 看似卡死。
- **v2.31.4**: 拔掉 `/tp-poi-enrich-monthly` schedule — batch 腳本 `scripts/poi-enrich-batch.ts` 在 v2.23.0 (commit `ac23d4e`) Google Maps Platform 切換時已刪，POI enrichment 改成「新 POI 即時 `POST /api/pois/:id/enrich`」+「30 天 daily refresh `scripts/google-poi-refresh-30d.ts` 50/day cap」，monthly batch 失去意義。移除 api-server 內 `scheduleDaily(8, 0, '/tp-poi-enrich-monthly', 'poi-enrich')` + `.claude/skills/tp-poi-enrich-monthly/` + `.codex/skills/tp-poi-enrich-monthly/`。剩 2 schedules：request-handler 每 30 min + daily-check 每天 09:00。
- **v2.31.3**: api-server 內建多排程 cron 取代 launchd / Cowork — v2.30.5 Cowork 路徑因 backend API 化 + JSON 重啟清空失效（2026-05-07 起 cron 全停 11 天），v2.30.18 single-cron band-aid 升級為 3 schedules 主路徑：`/tp-request` 每 30 min（兜底，CF Pages POST 即時 trigger 第一線）+ `/tp-daily-check` 每天 09:00 + `/tp-poi-enrich-monthly` 每天 08:00（skill 內 day-1 guard）。新增 `scripts/lib/schedule-daily.ts::computeNextDailyFire`（pure helper，3 case unit test）+ tp-daily-check / tp-poi-enrich-monthly skill Self-destruct section（tmux session orphan 避免）+ tp-request skill empty-queue path 也跳到 self-destruct（之前 30min orphan 阻擋下一輪 cron）。`spawnTmuxRequest(skillCommand)` + `processLoop(source, skillCommand)` 支援多 skill spawn。Deploy 後驗證點：api-server log 出現 3 行 `Scheduled <label>`。
- **v2.31.1**: AI 健檢 Phase 2 — Claude prompt 強化（5 個 audit dimensions: timing/distance/meals/sights/hotel + JSON-only enforcement 加 example + 加 `dimension` 與 `suggestion` 欄位）+ findings 顯示 polish（dimension chip 顯示審查維度、suggestion block 顯示建議怎麼修、`action_target.entry_id` → 「前往景點」navigate `/trip/:id/stop/:eid/edit` 優先於 day-only 跳轉）。Backend `sanitizeFindings` 加 VALID_DIMENSIONS allowlist + suggestion 字數限制（200 字）。Frontend `Dimension` type + `DIMENSION_LABEL` map（時間/移動/餐飲/景點/住宿 中文 label）。
- **v2.31.0** (migration 0067): AI 健檢功能上線 — 新表 `trip_health_reports`（PRIMARY KEY trip_id，per-trip latest only）+ POST/GET `/api/trips/:id/health-check` + 全頁 route `/trip/:id/health`（`TripHealthCheckPage.tsx`）。觸發後同步 INSERT `trip_requests` with `[AI 健檢]` prefix（user 在 /chat 可看對話留底），api-server processLoop 跑 Claude → PATCH `/api/requests/:id` 完成 hook 看到 prefix → parse reply JSON findings → UPDATE `trip_health_reports`。Findings schema `{severity: high|medium|low, title, description, action_target?: {day, entry_id}}`。Frontend polling 3s 直到 completed/failed。4 state（empty/pending/completed/failed）+ re-generating overlay（pending + 舊 findings → 「再重新生成」disabled）。命名注意：避開 v2.23.0 既有 `<TripHealthBanner>`（POI lifecycle health），新 component CSS prefix `tp-ai-health-*` / testid `ai-health-*`。兩處入口：TripCardMenu（列表卡片 ⋯）+ EmbeddedActionMenu（行程詳細頁 ⋯）。Severity 色用既有 `--color-priority-{high,medium,low}-{bg,dot}` token，不用 terracotta（accent 保留給 chrome / CTA）。
- **v2.30.0** (migration 0064): `trip_segments.mode_source` DROPPED — 移除「上鎖」概念。PATCH /segments/:sid 新 contract：`mode='transit'` 必填 min（source='manual'，不打 Routes），`mode='driving'`/`'walking'` 一律 Google Routes 重算（ignore body.min, source='google'）；缺 coords / API key → 保留舊 min/distance + computed_at=NULL 標 stale。recompute-travel skip 條件 `mode_source='user'` → `mode='transit'`；response field `pairsSkippedUser` → `pairsSkippedTransit`。Frontend 拔 🔒 icon、`isLocked` 變數、TravelPillDialog 「已手動覆寫」title indicator、EditEntryPage「重設為自動」button。Deploy 順序：先 apply migration → 再 deploy backend（30-90s race window accept，同 v2.29.0 pattern）。
- **v2.29.x**: trip_pois 整表 + saved_pois DROPPED — v2.29.0 (migration 0061+0062): `trip_pois` rip-out（10 col cleanup，所有 alternates/master/hotel/shopping/restaurant 統一 trip_entry_pois + trip_days.hotel_poi_id + poi_relations 模型）；v2.29.1 (migration 0063): `DROP TABLE saved_pois`（poi-favorites-rename Phase 2，10 天 soak 後）；v2.29.2: stale-travel ⚠ 偵測改用 `segment.computed_at IS NULL` signal（拔 Haversine vs distance divergence 邏輯）；v2.29.3: daily-check `queryRequestErrors` 移除 stale SELECT `trip_requests.mode`。
- **v2.28.3**: Entry multi-POI UI parity — 編輯頁、置換頁、行程一覽統一使用 `trip_entry_pois.sort_order=1` 正選 / `sort_order>1` 備選模型。`POST /alternates` 支援搜尋結果 find-or-create（不必先收藏），`PUT /poi-id` 保留搜尋 POI 的 type/category/address/rating/country。TimelineRail expanded choices 改讀 `entry.stopPois`，用通用「景點選擇」卡片顯示正選 + 備選，餐廳與一般景點格式一致。
- **v2.28.0** (migration 0059 standalone script): Restaurants → alternates Phase 1 — `scripts/migrate-0059-restaurants-to-alternates.ts` 把 `trip_pois` context='timeline' rows backfill 進 `trip_entry_pois` 當 alternates (sort_order > 1)。Idempotent (UNIQUE entry_id, poi_id)、`--dry-run/--apply`、`--local/--remote`、bump `entry_pois_version`。同時 fix GET /api/trips/:id/entries/:eid SELECT 只回 4 欄的 bug (改 SELECT * → time/startTime/endTime/note/poiId 都 surface)。Backend `fetchEntryPoisByEntries` 多 LEFT JOIN `trip_pois` surface restaurant 欄位 (hours/rating/price/reservation/reservation_url/description/note) 到 alternates response。EditEntryPage alternates row 加 rating star + price/hours/reservation chips (reservationUrl 存在 → link)。剩餘 Phase 2：觀察 2 週後 DROP `restaurants` TABLE，並移除 `raw.restaurants → infoBoxes` legacy read path。Deploy 順序：merge → backend 上線 → 手動 `--dry-run --remote` → 確認後 `--apply --remote`。
- **v2.27.0** (migration 0057 + 0058): multi-POI per entry — `trip_entry_pois` junction table (entry × poi M:N) + `trip_entries.entry_pois_version` INTEGER OCC counter。每 entry 可掛 1 master (sort_order=1) + N alternates。6 個新 endpoints（PATCH /master / POST,DELETE /alternates / PATCH /alternates/reorder）含 OCC `entryPoisVersion` token，409 STALE_ENTRY 表示需 refetch。PUT /poi-id 加可選 OCC + 拒絕 null（master invariant）。EditEntryPage 加 alternates section + master swap + 加備案 + 重排（V1 compact + expandable）。Frontend selector fallback chain (`getEntryMaster` / `getEntryMasterPoiId`) 處理 Phase 1 dual-read 過渡。Phase 2 (v2.27.x) 待 DROP `trip_entries.poi_id`。Deploy 順序：apply migration 0057 + 0058 → wait 30s → merge PR；rollback 必須先 revert backend deploy 再 DROP COLUMN（current backend reads `entry_pois_version` exclusively）。
- **v2.26.0** (migration 0056): `trip_entries` ADD COLUMN `start_time` / `end_time` TEXT — 拆分自 free-form `time` col。Backfill 既有 `"HH:MM-HH:MM"` 拆兩欄、`"HH:MM"` → start_time only。**不 drop** legacy `time`（dual-write 觀察期）。Backend dual-write：PATCH /entries 接受新舊欄位互轉；POST /entries / PUT /days / copy / poi-favorites add-to-trip 均同步寫 start/end + legacy time。新增 `EditEntryPage` 全頁編輯（route `/trip/:id/stop/:eid/edit`）— 三 sections（時間 / 從上一站移動方式 / 備註）整合 `PATCH /entries`（time + note）+ `PATCH /segments`（mode + min）並行儲存；TimelineRail toolbar pencil 從 inline note edit 改 navigate（testid `timeline-rail-edit-note-N` → `timeline-rail-edit-N`，inline note edit via note-value click 仍保留）。
- **v2.25.5** (migration 0055): `trip_pois.hours` DROP COLUMN → hours 純 `pois` master。Backfill `trip_pois.hours` non-null → `pois.hours`（COALESCE，不覆蓋既有），ALTER TABLE DROP。Backend cutover：`_merge.ts` 直接讀 `poi.hours`（不再 dual-read）；`POI_MASTER_ONLY_FIELDS` 加 `'hours'`，`PATCH /trip-pois` 帶 hours 自動 dispatch 到 pois；POST /trip-pois + PUT /days hours 透過 `findOrCreatePoi` 寫 pois.hours。**tp-* skills 完全重寫**：第一原則改為「`POST /api/pois/{id}/enrich` 透過 backend 打 Place Details API」，移除 `/browse` Google Maps + WebSearch 拼湊路徑。Place Details `weekday_descriptions` 已含全週時段 + 公休日（「星期三: 休息」），不需另外處理定休日欄位。Deploy 順序：先 merge PR + deploy backend，再 apply migration（順序顛倒會讓既有 backend `INSERT trip_pois.hours` 觸發 SQL fail）。
- **v2.25.4** (migration 0054 phase 1): `trip_pois.price` → `pois.price`（餐廳定價是客觀屬性，不該按 trip 變動）。Phase 1 = ADD COLUMN pois.price + COPY existing trip_pois.price。Backend cutover：write 走 pois.price（findOrCreatePoi + PATCH /pois 接受），read dual（`pois.price ?? trip_pois.price`）。`POI_MASTER_ONLY_FIELDS` 加入 `'price'` → PATCH /trip-pois 帶 price 自動 dispatch 到 pois。Migration 0055 觀察期後 DROP `trip_pois.price`。

- **v2.23.0** (migration 0051): Google Maps Platform 全套切換（OSM Nominatim + Mapbox + ORS + Leaflet + Haversine 全部 ripped out，no fallback）。`pois.osm_id` (number) → `pois.place_id` (string Google ChIJ id) + 4 lifecycle cols (`status` active/closed/missing + `status_reason` + `status_checked_at` + `last_refreshed_at`)。新表 `pois_search_cache`（24h TTL）+ `app_settings`（kill switch state + 90/50 hysteresis thresholds）。`functions/api/poi-search.ts` Nominatim → Google Places Text Search；`route.ts` Mapbox → Google Routes API；`OceanMap.tsx` Leaflet → Google Maps JS API（300-500KB lazy-loaded with `<MapSkeleton>` placeholder）。新 admin endpoints `/api/admin/{maps-lock,maps-unlock,backfill-status,maps-settings,quota-estimate,pois-pending-place-id,pois-due-refresh}.ts`。新 `/api/trips/:id/health` 給 `<TripHealthBanner>`。新 React 元件 `<PoiStatusBadge>` / `<TripHealthBanner>` / `<MapSkeleton>`。3 個 mac mini cron scripts（`google-poi-initial-backfill` / `google-poi-refresh-30d` / `google-quota-monitor`）+ npm scripts `backfill:google` / `refresh:google` / `quota:google`。tp-* SKILL.md (11 個檔 × .claude + .codex = 22 syncs) 改用 Place Details API（canonical curl block 在 `tp-shared/references/poi-spec.md`）。Hard cutover, no aliases.

- **v2.22.0** (migration 0050): `saved_pois` table → `poi_favorites`; `/saved` route → `/favorites`; `/api/saved-pois` → `/api/poi-favorites`; `SavedPoisPage` → `PoiFavoritesPage`; `AddSavedPoiToTripPage` → `AddPoiFavoriteToTripPage`. Hard cutover, no aliases. CSS class `tp-saved-*` → `tp-favorites-*`. Cross-skill auth header CF-Access → `Authorization: Bearer $TRIPLINE_API_TOKEN`.
- **v2.21.3** (migration 0049): `trip_requests.mode` column DROPPED. tp-request skill auto-classifies intent.
- **v2.21.2** (migration 0048): `trip_requests.mode` 改 nullable + drop CHECK constraint (phase 1 of mode rip-out).
- **v2.21.0** (migration 0046+0047): `trip_ideas` → `saved_pois` universal pool; `trips.owner_email` → `owner_user_id`; `saved_pois.email` / `trip_permissions.email` DROPPED.
- **v2.20.0** (migration 0046 phase 1): `trip_ideas` table retired; `tp-request mode` rip-out 啟動.
- **v2.19.x** (migration 0045): `pois.google_rating` → `rating`; `pois.maps` DROPPED; `trips.{auto_scroll,og_description,footer,food_prefs,is_default,self_drive}` DROPPED.
