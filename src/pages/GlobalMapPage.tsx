import Placeholder from '../components/shared/Placeholder';

export default function GlobalMapPage() {
  return (
    <Placeholder
      testId="global-map-page"
      eyebrow="Coming soon · Phase 3"
      title="所有行程地圖"
      body="跨 trip 的全域地圖 view — 看你所有 trips 的 polyline + POI 分布。跟單一 trip 的地圖（/trip/:id/map）不同。"
    />
  );
}
