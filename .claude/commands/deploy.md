Commit 所有變更並 push 到 GitHub，然後開啟 GitHub Pages 網頁確認。

步驟：
1. `git add` 有修改的檔案（不加 .claude/）
2. `git commit` 用繁體中文訊息描述改了什麼
3. `git push` 到 origin/master
4. 開啟瀏覽器 https://raychiutw.github.io/okinawa-trip-2026/ 確認
