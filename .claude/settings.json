{
  "hooks": {
    "afterWrite": [
      {
        "matcher": "memory/",
        "command": "npm run memory:export --silent"
      }
    ],
    "afterEdit": [
      {
        "matcher": "memory/",
        "command": "npm run memory:export --silent"
      }
    ]
  }
}
